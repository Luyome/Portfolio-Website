# Roadmap

A living status document for future Claude Code sessions: what's done, what's active, what's next. It does not repeat rules already defined in the other `/docs` files — see those for the "how."

## 1. Project Goal

The personal website of Ege Demir Ünal, combining Worldbuilding, Game Design, Portfolio, 3D/2D work, original stories, development journals, and interactive fictional-world exploration. First professional version targeted in approximately 2–3 weeks; the site continues receiving content and improvements after that, without requiring constant feature development (see `00_PRODUCT_VISION.md`, section 3).

## 2. Working Rules

- Work on one active sprint at a time.
- Do not begin future sprints unless explicitly instructed.
- Complete and validate the current sprint before moving forward.
- Do not silently expand scope.
- Record future ideas without treating them as current requirements.
- Prefer working, testable results over additional planning.
- Do not rebuild working systems without a clear reason.
- Update this roadmap whenever a sprint or major task is completed.
- Keep roadmap updates concise.
- Detailed decisions belong in their relevant documentation files, not here.

## 3. Sprint Status

**Sprint 0 — Foundation** — *Complete.* Product, content, design, component, motion, and technical documentation (`AI_RULES.md` through this file).

**Sprint 1 — Core Systems** — *Complete.* Inspected, preserved, aligned, and completed the shared systems the site already had — not a from-zero build. Delivered: public UI foundation, accessibility and responsive core, validation and search/filter infrastructure, SEO/sitemap/robots, media optimization, performance and Vercel/Neon usage QA, and local and production integration QA.

- Task 1.1 — Existing Core Systems Audit: **Complete.**
- Task 1.2 — Final Product Scope and Documentation Alignment: **Complete.**
- Task 1.2.1 — Documentation Consistency Patch: **Complete.**
- Task 1.3 — Reduced-Motion Foundation: **Complete.**
- Task 1.4 — Spacing Tokens Alignment: **Complete.**
- Task 1.5 — Typography Tokens Alignment: **Complete.**
- Task 1.6 — Shared Public UI Primitives: **Complete.**
- Task 1.7 — Loading, Empty & Error States: **Complete.**
- Task 1.8 — Error Boundaries & Fallback Behaviour: **Complete.**
- Task 1.9 — Content Validation Foundation: **Complete.**
- Task 1.10 — Search, Tags, Categories & Archive Foundation: **Complete.**
- Task 1.11 — SEO Metadata Foundation: **Complete.**
- Task 1.12 — Sitemap, Robots & Indexing Rules: **Complete.**
- Task 1.13 — Media Optimization Foundation: **Complete.**
- Task 1.14 — Accessibility & Responsive Core QA: **Complete.** — fixes implemented and validated (type-check/lint/build pass); desktop, keyboard/modal, and mobile/tablet viewport QA (390×844, 768×1024, via DevTools device emulation) all performed in-browser. Mobile viewport testing found and fixed a real overflow bug: `.sk-grid` (Sketches + 3D pages) used a fixed 4-column layout with no `min-width:0` on grid items, causing images to force horizontal overflow below ~540px; fixed with `min-width:0` on `.sk-item` and a 2-column override at the existing 820px breakpoint. Also fixed: MapZoomPanel toolbar/breadcrumb overlap at ~390px, GalleryModal's mobile single-column grid collapsing `.gm-img-side`'s row to 0px (image rendering on top of the info panel), and the tablet-only (768–1023px) header row overflowing its own viewport by ~50px (last nav/social links unreachable).
- Task 1.15 — Performance & Vercel Usage QA: **Complete.** — code audit (client bundle, DB queries, rendering/cache, media transfer) plus read-only Vercel and Neon dashboard review found the site well within plan limits and free of N+1/duplicate-query patterns except one: `getSiteSettings()` was read 2-3x per request (root layout, site layout, and — on the Home Page — the page itself again). Fixed by wrapping it in React's `cache()`, per the local Next.js 16 docs' documented dedup pattern for non-`fetch` ORM reads (`node_modules/next/dist/docs/.../caching-without-cache-components.md`, "Deduplicating requests") — zero behavior change, one file (`lib/site-settings.ts`). No other verified low-risk fix met the bar; GalleryModal's `<img>` usage, the `.sk-grid` masonry thumbnails, and Archive's same-year ordering were reviewed and left alone (masonry/GalleryModal excluded by task scope and Task 1.14's recent a11y work; ordering has no perf benefit). Type-check, lint (0 new warnings), build, and diff-check all passed; 8-route smoke QA (including GalleryModal and the World Map panel) passed with no console errors.
- Task 1.16 — Sprint 1 Integration QA: **Complete.** — resumed after an unrelated system crash mid-QA; git status was clean (no half-finished edits, no roadmap corruption), so this was a clean continuation, not a rebuild. Re-ran type-check/lint (0 errors, the same pre-existing 27 warnings)/build/`git diff --check`, all clean. 8-route smoke QA (console, header/footer, single `h1`, no page-level overflow), keyboard/ARIA checks (skip-to-content, Work menu open/Escape/focus-return, `aria-current`, mobile-menu toggle wiring, Archive's `aria-pressed` filters), GalleryModal + ImageZoomOverlay + MapZoomPanel (open/close/Escape-nesting/focus-return/zoom/reset), Worldbuilding search (trim + case-insensitive + empty-search no-op), Archive year/type filters and invalid-`year`-param fallback, 404 + its Home/Archive links, admin login `noindex,nofollow`, sitemap.xml/robots.txt (dev correctly disallows all; prod allows public routes and blocks `/admin/`+`/api/`), and the Task 1.15 `getSiteSettings()` cache all passed with no regressions. Production (`portfolio-website-eight-xi-79.vercel.app`) confirmed Ready on the latest deploy, aliased correctly, and `/`, `/portfolio`, `/worldbuilding`, `/about`, `/sitemap.xml`, `/robots.txt` all return 200 with unique titles and no error markers. **390px/768px responsive regressions — now verified in a follow-up session:** the browser extension's window resize still could not shrink the real viewport below desktop width (confirmed again, same root cause as before — OS/window-manager level, not fixable from the page-automation tool), so verification instead used a scripted headless Chrome session driven directly via the Chrome DevTools Protocol (`Emulation.setDeviceMetricsOverride`, the same primitive DevTools' own device toolbar uses), launched from the local Chrome install with no new project dependency. Checked `document.documentElement.scrollWidth` vs `clientWidth` plus visual screenshots on production for Home, Worldbuilding (including MapZoomPanel opened), Portfolio (including GalleryModal opened), and Archive, at both 390×844 and 768×1024: no page-level horizontal overflow on any of the 8 combinations, mobile hamburger menu and stacked layouts render correctly, GalleryModal's image/info stacking and MapZoomPanel's zoom toolbar both fit within viewport with no regressions from Task 1.14's fixes. One benign finding, not a bug: Worldbuilding's `.ph-wm` map-preview thumbnail measures 421px `scrollWidth` inside its 390px container (31px larger than its box) but is clipped by the container's own `overflow:hidden`, so it never affects page scrollWidth or visible layout — left as-is per scope (no visible defect to fix).
- Task 1.17 — Roadmap Status Update / Sprint Close: **Complete.**

Existing systems confirmed and to be preserved (Task 1.1): root and site layouts, Header, Footer, owner-only Admin authentication, Admin dashboard, existing CRUD systems, Home Page foundation, Portfolio, Worldbuilding browser, public map foundation, Admin Map Editor, Archive, About page.

Confirmed Sprint 1 gaps to close: reduced-motion support, spacing tokens, typography tokens, shared public UI primitives, loading states, empty states, error states, error boundaries, SEO metadata, sitemap, robots configuration, content validation, media optimization, integration QA.

Comments, likes, bookmarks, notifications, and public accounts are not part of this sprint — see section 6, Explicitly Out of Scope.

**Sprint 2 — Private Admin Panel and CMS Refinement** — *Complete.* Admin authentication, dashboard, CRUD, and a working Map Editor already exist. This sprint refines and extends them (media management polish, tag/category management, content relationship management, Map Manager improvements) rather than building an admin system from scratch.

- Task 2.1 — Admin Panel Audit & Scope Lock: **Complete.** Audited existing Admin routes, authentication, CRUD flows, forms, server actions, media upload system, and database relationships (code review + logged-in browser walkthrough). See **Verified Audit Findings** below.
- Task 2.2 — Admin Shell, Navigation & Responsive Layout: **Complete.** Added an accessible mobile/tablet Admin navigation replacement (`components/admin/AdminSidebar.tsx`): a fixed top bar (hamburger + active-section label) plus a slide-in drawer, shown only below the existing 820px sidebar-hide breakpoint — the desktop `.adm-sb` sidebar and its active-state logic are unchanged and still render as-is above 820px. Drawer behavior verified via real headless-Chrome CDP interaction (`Emulation.setDeviceMetricsOverride`, same technique as Task 2.1's follow-up pass, session authenticated with a locally-minted JWT — `ADMIN_PASSWORD` never read): `aria-expanded`/`aria-controls`/dynamic `aria-label`, Escape closes and returns focus to the burger button, backdrop click closes and returns focus, link click closes and navigates, body scroll locked while open and restored on close, all 14 nav links (13 sections + Back to Site) reachable and correctly reflect the active route including nested ones (`/admin/worldbuilding/map` → "Worldbuilding" active). No new dependency and no added animation beyond the existing `hn-burger`-style icon-morph transition already used by the public Header. Fixed the three confirmed overflow bugs: added a reusable `.adm-table-wrap` (scrollable, `overflow-x:auto`) around every `.adm-table` instance site-wide (16 admin list/panel tables, not just the two required ones, since the underlying `.adm-table` bug was identical everywhere) plus a ≤820px rule switching `.adm-table` from fixed to auto column layout so Title/name columns size to content instead of a fixed 130px budget; added `flex-wrap:wrap` to `.adm-field-label-row` so `FieldStyleControls` pills drop to their own line instead of overflowing. Verified via the same CDP session at 390×844, 768×1024, and 1440×900 on Dashboard, Portfolio list, Portfolio create form, and Map Editor: zero page-level horizontal overflow at any width/page (`scrollWidth`/`clientWidth` measured, not assumed), Map Editor's 6-column Pins table fully readable with no scrollbar at 768px, style pills wrap cleanly at 390px, and the 1440px desktop views are visually unchanged from before (sidebar intact, no mobile top bar). Type-check, lint (0 errors, the same pre-existing 27 warnings, no new ones), production build, and `git diff --check` all passed; only the 18 intended files changed.
- Task 2.3 — Admin Dashboard & Content Overview: **Complete.** Turned the Admin home page into a functional hub for daily content management, preserving the existing stat cards, Content Activity chart, and dark-mode toggle. Added: a header "View Public Site" link (new tab) plus a single primary create action chosen from the content type with the most existing items; a full "Content Overview" grid (Portfolio, Sketches, 3D Models, Worldbuilding, Games, Maps, Services — Maps was the confirmed gap, added to `getDashboardData()`'s `sections`) with real counts linking to each list page; a "Quick Actions" grid of the 6 working create/editor routes; a "Recent Content" list sourced from a new `getRecentContent()` helper (bounded, deterministic `ORDER BY created_at DESC LIMIT 8` per table, merged) — the confirmed missing per-item recent list; and an "Attention Needed" list (only rendered when non-empty) flagging rows whose mutation-boundary-required title/label field (`lib/actions/*.ts`'s `requiredText`) is empty — the confirmed missing warning system, deliberately excluding the `img` field since every action treats it as optional today. Fixed the Dashboard's `<div className="adm-title">` and `DashboardChart`'s `<div className="adm-chart-title">` to real `<h1>`/`<h2>` elements (visually unchanged) so the page has exactly one `h1` and a real heading hierarchy. No new dependency, schema, migration, or analytics tracking.
- Task 2.4 — Shared Admin Form System: **Complete.** Consolidated three genuinely repeated Admin form structures into new, small, focused components — no new form framework. `components/admin/Field.tsx`: wraps a single native `<input>`/`<textarea>`/`<select>` with a `<label htmlFor>`, a textual **Required**/**Optional** indicator (never a bare asterisk), optional help text and error text with unique, correctly-wired `id`/`aria-describedby`/`aria-invalid`, and an optional `labelExtra` slot (replaces the old bespoke `.adm-field-label-row` + `FieldStyleControls` pairing). Deliberately has no client-only APIs (pure `cloneElement`), so it renders identically from Client Component forms (Portfolio/Sketches/3D/Games/Worldbuilding/Maps/About, which need live-preview state) and Server Component pages (Site Settings, Archive) — the render-prop version tried first broke the Settings page build (functions aren't serializable across the Server/Client boundary) and was replaced with this approach. `components/admin/FormError.tsx`: the single form-level error banner every `useActionState` form repeated inline, now with `role="alert"` (previously missing everywhere). `components/admin/FormActions.tsx`: wraps `SaveButton` (unchanged — already handled pending/double-submit correctly) with a new Cancel/back-to-list link, the confirmed missing piece. Custom picker widgets (YearPicker, DatePicker, OrderPicker, ColorPicker, NumberPicker) and upload fields (ImageUploadField, FileUploadField) always carry a valid default/are upload-mechanics — left on their existing bare `.adm-field` markup, untouched, per scope. Migrated: PortfolioForm, GameForm, SketchForm, Model3DForm, WorldbuildingForm, WorldMapForm, ServiceForm, AboutForm, the Site Settings page, and the Archive "Add Category" form (whose Add button was a raw `<button>` with no pending state — now `SaveButton`). PageAppearanceForm (color-grid, no text fields) and MapPinEditPanel (a controlled non-`<form>` panel, Task 2.8 territory) were reviewed and deliberately left alone — no genuine Field-shaped consumer there. No field `name`, hidden input, default-value, server action, redirect, or CRUD behavior changed anywhere. Verified via a real headless-Chrome session (Puppeteer against the local Chrome install, no new project dependency — installed `--no-save` for the QA run and pruned back out afterward) authenticated with a locally-minted session JWT (`lib/auth.ts`'s `createSessionToken()`, via a temporary `/api/devauth-qa-temp` route deleted before this task closed — `ADMIN_PASSWORD` never read or used): Portfolio/Worldbuilding/Games/Maps create forms and the Site Settings page all measured **zero** `scrollWidth`-vs-`clientWidth` overflow at 390×844, 768×1024, and 1440×900, with screenshots confirming clean Required/Optional badges, wrapped hint text, and a non-overlapping Save+Cancel row at every width; the existing 820px mobile nav (Task 2.2) and 1440px desktop sidebar were unaffected. Also confirmed, non-destructively: submitting Portfolio's create form with an empty required Title field renders the new `role="alert"` `FormError` banner ("Title is required.") and — per code review of `lib/actions/portfolio.ts`'s `readFields()`, which throws before any `db.insert` — writes nothing; the Portfolio list's 5 existing rows were unchanged afterward. Type-check, lint (0 errors, the same pre-existing 27 warnings, no new ones), production build, and `git diff --check` all passed; only the 14 intended files changed.
- Task 2.5 — Permanent Browser QA Harness: **Complete.** `scripts/responsive-qa.mjs` (`npm run qa:responsive`): dependency-free headless Chrome/CDP responsive smoke QA over Node's built-in `fetch`/`WebSocket`, no Puppeteer/Playwright. Launches Chrome via `CHROME_PATH` or a common Windows install path with `--headless=new` and a fresh temp `--user-data-dir`/dedicated CDP port, checks `QA_BASE_URL` (default `http://127.0.0.1:3000`) reachability first, tests `QA_ROUTES` (default `/`, `/portfolio`, `/worldbuilding`, `/about`, `/admin/login`) at exactly 390×844, 768×1024, and 1440×900 via `Emulation.setDeviceMetricsOverride`, and reports per-route/viewport horizontal overflow (with the top offending elements), console errors, and uncaught exceptions as one `PASS`/`FAIL` line each, closing only the Chrome process it launched. This machine's headless Chrome (`150.0.7871.189`) crashes intermittently under sustained CDP automation regardless of flags (reproduced in isolation, unrelated to this script's logic — not a Windows Defender or enterprise-policy interaction, per Application/Defender event-log review); the harness tolerates this with up to 2 automatic same-session relaunch-and-retry attempts before failing outright, verified by real runs recovering from 1–2 mid-run crashes to a clean 15/15 pass. Validated: type-check (0 errors), lint (0 errors, the same pre-existing 27 warnings), production build, and `git diff --check` all passed; only `scripts/responsive-qa.mjs`, `package.json` (new `qa:responsive` script), and `CLAUDE.md` (new Responsive QA section) changed.
- Task 2.6 — Admin Authentication & Destructive Action Safety: **Complete.** Every owner-only mutation in `lib/actions/*.ts` (91 exported functions across 13 domain files: about, appearance, archive-categories, cv, games, home, map, models3d, portfolio, services, settings, sketches, worldbuilding) now calls a new shared `requireAdminSession()` (`lib/actions/guard.ts`) as its first statement, before any input parsing or database mutation — closing the gap where `proxy.ts`'s `/admin/:path*` matcher protects Admin *pages* but a Server Action is invoked by its action ID independent of the page route that imports it, so an unmatched-path POST carrying a valid `Next-Action` header was not guaranteed to pass through the proxy. The guard reads the existing `admin_session` JWT cookie, reuses `lib/auth.ts`'s existing `verifySessionToken`, and redirects to `/admin/login` on a missing/invalid session (no new auth system, no logged values). `app/api/admin/blob-upload{,-file}/route.ts` already had their own equivalent inline session check pre-existing this task and were left as-is (still return 401 unauthenticated, verified live). `auth.ts`'s `loginAction`/`logoutAction` are the auth entry points themselves and are intentionally unguarded. No `next`/`returnTo`-style redirect parameter exists in the login flow, so no open-redirect surface to fix. Cookie flags (`httpOnly`, `secure` in production, `sameSite: lax`, `path: /`, matching `maxAge` on set/delete) were already correct and left unchanged. Existing `window.confirm` delete flows, ID validation before mutation, and `safeErrorMessage` (`lib/validation.ts`) error handling were already in place and untouched — no new soft-delete/undo system added. Verified live with the dev server: `/`, `/portfolio`, `/about` return 200; `/admin` and `/admin/portfolio` redirect once (307) to `/admin/login` with no loop; `/admin/login` returns 200. Runtime unauthenticated *mutation* testing (POSTing a real Server Action without a session) was not performed — it requires the build's per-action encrypted `Next-Action` ID, which isn't safely obtainable without either a real authenticated session or a temporary bypass route, both excluded by this task's rules; static guard coverage (91/91 functions) plus the verified redirect behavior stand in for it. Type-check (0 errors), lint (0 errors, same pre-existing 27 warnings), production build, `git diff --check`, and `npm run qa:responsive` on `/,/admin/login` (6/6 pass) all passed; only the 13 `lib/actions/*.ts` files plus new `lib/actions/guard.ts` changed, no new dependency or lockfile change.
- Task 2.7 — Media Upload Safety: **Complete.** New `lib/upload-policy.ts`: a single source of truth (pure, dependency-free — importable from both "use client" components and Route Handlers) for three categories matching the upload flows that actually exist — `image` (JPEG/PNG/WebP/GIF/AVIF, 10MB) via `/api/admin/blob-upload`; `document` (PDF or image, 15MB) for the CV upload; `gameBuild` (ZIP/7z/RAR/EXE/DMG/GZ/TAR/PDF, 300MB, preserving the pre-existing, intentional downloadable-game-build format list — a deliberate exception to the "no executables/archives" default, confirmed with the owner since it's a real, working site feature, not a new capability) — both sharing `/api/admin/blob-upload-file`, disambiguated by a `clientPayload` category tag the client sends and the server independently re-validates. Provides `checkUpload()` (extension + size, returns a safe file name and the canonical Content-Type — never the browser-reported one), `sanitizeFileName()` (basename-only, strips control characters and path-traversal sequences, truncates to 100 chars), `acceptAttrFor()`, and `describeUploadPolicy()`. Both Blob routes now validate in `onBeforeGenerateToken` (before any client token is issued, so an invalid file never reaches Blob): admin session first (unchanged 401 behavior), then the pathname's extension is resolved to a narrow, category-specific `allowedContentTypes` list (rejecting anything outside it) plus `maximumSizeInBytes` (enforced by Blob itself at the actual PUT, since file bytes never pass through this Next.js server in the client-upload architecture already in place) — a caught rejection returns a generic 400 with no internal detail. `ImageUploadField`, `FileUploadField` (new required `category` prop), and the two remaining inline upload spots (`ContentEditor`'s "Insert Image", `MapPinEditPanel`'s pin image) all run the same `checkUpload()` client-side before any bytes leave the browser, show a short `role="alert"` error tied to the input via `aria-describedby`, reset the file input on every selection (including failures) so the same file can be re-picked, and — for `MapPinEditPanel`, which has its own Save button rather than a form submit — disable Save while an upload is in flight. PSD/PSB/BLEND/EXR and any other source/master formats remain unsupported (never were), and no new file type was introduced anywhere. No alt-text field exists in the schema for any image column — confirmed via `db/schema.ts` — so this stays out of scope, matching Task 2.6's finding; deferred to whichever future sprint adds it. The orphaned-Blob problem noted in Task 2.6 is unchanged: uploads and the record mutation that references them are two separate requests in this architecture (the client uploads to Blob first, then submits a normal form with the resulting URL), so there is no single request where "Blob succeeded, then a mandatory step failed" — a safe, scoped cleanup has no request to attach to, and a general sweep is out of this task's bounds by design; still unaddressed. Verified: a standalone script exercising `checkUpload`/`sanitizeFileName`/`acceptAttrFor`/`allowedContentTypesForPathname` (12 cases: valid image accepted, `.exe` rejected for `image`, double-extension resolves by the last one, `.sh` rejected for `gameBuild`, oversized file rejected, path traversal stripped, long name truncated, `accept` lists match policy, category cross-checks) all passed, then was deleted; a live dev-server request confirmed both routes still return `401 {"error":"Unauthorized"}` with no cookie and no internal detail. Type-check (0 errors), lint (0 errors, same pre-existing 27 warnings, no new ones), production build, `git diff --check`, and `npm run qa:responsive` on `/,/admin/login` (6/6 pass, run via PowerShell — Git Bash's leading-`/` path mangling produced a false failure on `/` alone) all passed; only the 8 files above plus new `lib/upload-policy.ts` changed, no new dependency or lockfile change.
- Task 2.8 — Existing CRUD Consistency: **Complete.** Audited create/update/delete across Portfolio, Sketches, 3D, Games, Worldbuilding, Maps, Services, About, CV, Archive/categories, Settings, Appearance, and Home. Fixed two concrete, repeated inconsistencies: (1) **Invalid edit-ID handling** — all 7 dynamic Admin edit routes (`services`, `sketches`, `3d`, `games`, `worldbuilding`, `worldbuilding/maps`, `portfolio`) parsed the route param with bare `Number(id)` and queried the database directly; a non-numeric ID (e.g. `/admin/services/abc/edit`) produced `NaN`, which Neon/Postgres rejected as an uncaught `Failed query ... params: NaN` error instead of the intended `notFound()` — confirmed with a standalone, read-only script against the real database, then deleted. Every route now parses the ID with the existing `requiredId()` (`lib/validation.ts`) inside a `try/catch` and calls `notFound()` on failure, before any query runs; a valid-but-missing ID still hits the same, already-correct `if (!item) notFound()` path — unchanged. (2) **Empty states** — all 8 Admin list pages (Portfolio, Sketches, 3D, Games, Worldbuilding, Maps, Services, Archive categories) rendered a table with only a header row and zero explanatory text when a content type had no records (the gap flagged in Task 2.5). New `components/admin/AdminEmptyState.tsx` (reuses the existing public `.empty-state`/`.empty-state-desc` classes from `components/EmptyState.tsx` — no new design system) now renders in place of the table when the list is empty; the existing "+ New X" create link above each table was already present and untouched. Also fixed: **SaveButton double-submit** — added a synchronous `useRef` guard in `components/admin/SaveButton.tsx` that blocks a second click's default form submission before React's `pending`/`clicking` state has re-rendered (the "small window" noted in the Task 2.1 audit), without changing labels, styling, or the two existing usage modes. **Deterministic ordering** — the 8 primary Admin list queries ordered only by `sortOrder` (and, for date-sorted lists, `year` then `sortOrder`), with no tie-break for rows sharing the same value (e.g. multiple items defaulting to `sortOrder: 0`); added `asc(<table>.id)` as a final, stable tie-break to each, without changing any existing user-set order. Reviewed and found already consistent, no change needed: `requireAdminSession()` guard ordering (91/91 functions, unchanged since Task 2.6), `redirect()`/`notFound()` never caught by a surrounding `try/catch` anywhere, delete actions never leave the admin on a since-deleted edit page (delete is only ever invoked from list rows, never from within an edit page), and `revalidatePath` targets per content type. Noted but explicitly out of scope (would require touching the validation/form-field system per Task 2.4/2.9 boundaries): `services.ts`, `archive-categories.ts`, `cv.ts`, `settings.ts`, `appearance.ts`, and `home.ts` skip the `readFields()`-in-`try/catch`-returning-`ActionState` pattern that Portfolio/Sketches/3D/Games/Worldbuilding use, relying on HTML5 `required` only — pre-existing, documented under Task 2.4's findings, not a redirect/revalidate/CRUD-flow defect. Sub-resource (image/link/video panel) ordering ties were not touched — lower-visibility, out of this task's priority order. Verified: type-check (0 errors), lint (0 errors, same pre-existing 27 warnings, no new ones), production build, `git diff --check`, and `npm run qa:responsive` on `/,/admin/login` (6/6 pass, dev server started and stopped by its own PID) all passed; only the 16 files above plus new `components/admin/AdminEmptyState.tsx` changed, no new dependency or lockfile change.
- Task 2.9 — Controlled Metadata Foundation & Manager: **Complete.** New `metadata_options` table (`db/schema.ts`): `type` (plain app-validated text — matching the schema's existing convention for controlled string sets, e.g. `kind`/`pinType`, rather than introducing this codebase's first pgEnum), `name`, `slug`, `iconUrl`, `websiteUrl` (Software-only, `null` for every other type), `sortOrder`, `isActive`, `createdAt`, `updatedAt`, plus a `uniqueIndex` on `(type, slug)` — same slug may repeat across different type groups, never within one. Purely additive migration `drizzle/0018_ancient_jack_flag.sql` (one `CREATE TABLE`, one `CREATE UNIQUE INDEX`), applied via `drizzle-kit migrate`. The four allowed values (`medium`, `subject`, `software`, `tag`) live in exactly one place, `lib/metadata.ts` (`METADATA_TYPES`/`MetadataType`/`METADATA_TYPE_LABELS`/`isMetadataType`). New `lib/actions/metadata.ts`: `createMetadataOption`/`updateMetadataOption` (both `useActionState`-shaped, redirecting to `/admin/metadata?type=…` on success), `toggleMetadataOptionActive`, `updateMetadataOptionSortOrder`, `deleteMetadataOption` — every one calls the existing `requireAdminSession()` first. Slug is normalized via the existing `slugify()` (`lib/content-blocks.ts`, reused rather than duplicated) from a manual value or, if blank, from Name; duplicates within the same type are rejected with a safe message before insert/update. `updateMetadataOption` never trusts a `type` field from the submitted form — it always re-reads the existing row's type from the database, so edit can't move an option between groups even with a tampered request. New `nullableHttpUrl()` (`lib/validation.ts`) restricts Software's Website URL to `http:`/`https:` only (rejects `javascript:`, `data:`, and unparsable/relative values); the icon reuses the existing `ImageUploadField`/`nullableUrl`, no new upload route. New `lib/metadata-usage.ts`: `getMetadataUsageCount()`, a single extension point that returns `0` today (no Portfolio or other junction table exists yet — Task 2.10 scope) and is already what both the list view's Usage column and `deleteMetadataOption`'s in-use guard call through. New Admin route `/admin/metadata` (`app/admin/(dashboard)/metadata/page.tsx`): four groups selected by a `?type=` query tab (no nested route tree), list table (Name, Slug, Status toggle, Sort order via the existing `NumberPicker`+`SaveButton` row pattern, Usage, Icon preview for Software, Edit/Delete) ordered `sortOrder ASC, name ASC, id ASC`, `AdminEmptyState` when a group is empty, and a same-page create/edit form (`?edit=<id>`) via new `components/admin/MetadataForm.tsx` using `Field`/`FormError`/`FormActions`/`ImageUploadField` — Software-only fields (icon, website) render only for that group. One new nav link (`lib/admin-nav.ts`, under "Archive"). Verified with a temporary, non-destructive lifecycle script run directly against the database (`tsx`, no auth-bypass route) and deleted afterward: create → read → update → toggle-inactive → sort-order update → delete all passed; a duplicate `(type, slug)` insert was correctly rejected by the DB unique index while the same slug under a different type was correctly allowed; `metadata_options` row count returned to 0 before/after; `portfolio_items`/`sketches`/`models_3d`/`worldbuilding_entries`/`games`/`world_maps`/`map_locations` row counts were confirmed unchanged. Authenticated in-browser QA of `/admin/metadata` itself was out of scope per this task's own rules (no auth bypass, no temporary backdoor); route compilation, the direct database lifecycle test, and code review of the guard/validation paths stand in for it, consistent with Task 2.6's precedent. Type-check (0 errors), lint (0 errors; one new `no-img-element` warning on the Software icon preview, consistent with every other existing Admin preview thumbnail in this codebase — 28 total warnings, 27 pre-existing + this one), production build, `git diff --check`, and `npm run qa:responsive` on `/,/portfolio,/about,/admin/login` (12/12 pass, run via PowerShell per Task 2.7's precedent) all passed; no new dependency or lockfile change. Portfolio form wiring, multi-select, chips, and legacy Medium/Software/Tags data migration are explicitly Task 2.10, not started here.
- Task 2.10 — Portfolio Metadata Selectors & Legacy Migration: **Complete.** New Portfolio-specific junction table `portfolio_metadata_options` (`db/schema.ts`): `portfolioId`/`metadataOptionId` FKs (both `onDelete: cascade`), a unique index on `(portfolioId, metadataOptionId)`, and one index per FK column — no polymorphic `contentType/contentId` table, no per-type junction tables. Purely additive migration `drizzle/0019_goofy_karnak.sql` (one `CREATE TABLE`, two FKs, three indexes — no drops/truncates/data changes), applied via `drizzle-kit migrate`. Real production data was read (read-only) before writing the backfill to confirm actual separators: `medium` never contained a comma/slash/semicolon in any of the 5 existing rows (always one freeform value — never split); `software` always used `", "` to join multiple tools (e.g. "Maya, ZBrush, Marvelous Designer" — split on comma); `tags` was already a native array column (one tag per element, no split needed); Subject Matter has no legacy column and no values were invented for it. New `scripts/backfill-portfolio-metadata.ts` (kept in the repo, added as `npm run db:backfill-portfolio-metadata` — explicitly named, idempotent, non-destructive, Portfolio-only): matches/reuses an existing `metadata_options` row by `(type, slug)` before creating one, matches/reuses an existing junction row by `(portfolioId, metadataOptionId)` before inserting one, and never touches `portfolio_items` or its legacy columns. Verified idempotent by running it twice against the real database: run 1 created 26 metadata options (3 medium, 7 software, 16 tag) and 37 junction rows across all 5 Portfolio items; run 2 created 0 of either. Post-backfill verification (read-only queries): Portfolio row count unchanged (5), all 5 rows' legacy `medium`/`software`/`tags` values unchanged, 0 duplicate `(portfolioId, metadataOptionId)` pairs, 0 orphan junction rows, 0 junction rows pointing at a mismatched/invalid option type, 0 duplicate `(type, slug)` pairs, and (via `getMetadataUsageCount`) all 26 backfilled options show real usage ≥ 1 so `deleteMetadataOption`'s in-use guard correctly rejects deleting any of them. New `lib/portfolio-metadata.ts` (Portfolio-scoped, not a general repository layer): `getActiveMetadataOptionsByType()` (one query, active options grouped by type, for the create/edit form's pickable pool), `getPortfolioMetadataSelections()` (one join query, an item's current selections grouped by type — including any since-made-inactive option, so edit never silently drops it), and `readMetadataSelections()` (validates every submitted ID actually exists and belongs to the claimed type, dedupes, rejects unknown/cross-type IDs, returns both IDs for the junction table and names for the legacy mirror). `lib/metadata-usage.ts`'s `getMetadataUsageCount()` now runs a real `COUNT` against `portfolio_metadata_options` instead of the Task 2.9 placeholder `0`. New `components/admin/MultiSelect.tsx`: dependency-free searchable multi-select (search input + filtered dropdown + removable chips), reused across all four metadata fields; a previously-selected-but-now-inactive option still renders as a removable "Inactive"-badged chip in the edit form but is excluded from the addable list; an empty option pool shows a `/admin/metadata?type=…`-linked empty state instead of an empty dropdown; no free-text/comma-separated entry point exists anymore. `PortfolioForm`'s Medium/Subject Matter/Software/Tags free-text inputs are replaced by this component (all four optional — empty selection is valid); the live preview's tag chips now derive from the Tags `MultiSelect`'s selection instead of a raw text field. `lib/actions/portfolio.ts`: `createPortfolioItem` inserts the Portfolio row, then inserts its junction rows — the neon-http driver has no cross-statement transaction support (`db.transaction()` itself throws "No transactions support in neon-http driver", confirmed by reading its source before deciding this), so if the junction insert fails, the just-created row is deleted as a compensating action (cascade takes any partial junction rows with it) rather than leaving a half-related item behind. `updatePortfolioItem` diffs the existing vs. submitted selection and runs the row update plus the junction add/remove as one `db.batch()` call — a real atomic HTTP-level Postgres transaction, the neon-http driver's actual substitute for `db.transaction()`. Both actions keep the legacy `medium`/`software`/`tags` columns as a compatibility mirror of the selected option names (Section 8) — public Portfolio pages read those columns unchanged and were confirmed still rendering correctly (tags visible on `/portfolio`) after the migration. Verified: type-check (0 errors), lint (0 errors, same pre-existing 27 warnings, no new ones — 28 total including Task 2.9's pre-existing Software-icon-preview warning), production build, `git diff --check`, and `npm run qa:responsive` on `/,/portfolio,/admin/login` (9/9 pass, run via PowerShell per Task 2.7/2.9 precedent) all passed; no new dependency, no lockfile change. Interactive in-browser Admin QA of the new selectors themselves (Section 12) was not performed against a fresh session per this task's own explicit rule (no auth bypass, no minted JWT/cookie) — component compilation, the direct database lifecycle checks above, and code review of the validation/transaction paths stand in for it, consistent with Task 2.9's precedent for the same constraint.
- Task 2.11 — Admin Integration QA: **Complete.** Integrated QA of every Sprint 2 system together — no new feature, data model, design, or refactor. Static review confirmed every exported function in `lib/actions/metadata.ts` (5) and every exported function in `lib/actions/portfolio.ts` (12) calls `requireAdminSession()` first — corrected from this task's original "6"/"17" (Sprint 2 Closure Audit finding 11A; the higher original counts didn't match the actual file contents); both Blob upload routes (`/api/admin/blob-upload{,-file}`) independently verify the session and apply `lib/upload-policy.ts`'s category-specific allow-lists before issuing a token; `portfolio_metadata_options` has cascading FKs on both columns, a unique `(portfolioId, metadataOptionId)` index, and one index per FK; `readMetadataSelections()` (`lib/portfolio-metadata.ts`) validates every submitted ID exists, matches its claimed type, and dedupes before `createPortfolioItem`/`updatePortfolioItem` write the junction rows (delete-and-recreate via `db.batch()` for update, a compensating delete on junction-insert failure for create); `getMetadataUsageCount()` runs a real `COUNT` against the junction table and `deleteMetadataOption` no-ops when usage > 0; legacy `medium`/`software`/`tags` columns are unchanged (`notNull`, still dual-written); no public filtering, ArtStation shell, or Sprint 3 code found anywhere. Read-only database integrity queries against the real database all passed: 0 orphan junction rows, 0 duplicate junction pairs, 0 type-mismatched junction rows, 0 duplicate `(type, slug)` pairs, Portfolio row count still 5, `metadata_options` still 26, `portfolio_metadata_options` still 37 (all matching Task 2.10's ending counts), legacy columns present, other content tables' row counts unchanged — verified via a temporary read-only script (`scripts/qa-2.11-integrity-temp.ts`, no writes), deleted after use. Runtime QA against a locally running dev server: `/`, `/portfolio`, `/worldbuilding`, `/about`, `/admin/login` all 200; unauthenticated `/admin`, `/admin/portfolio`, `/admin/metadata` each a single 307 to `/admin/login` (no loop, confirmed with `-L`); both Blob routes return `401 {"error":"Unauthorized"}` with no cookie; no secret/internal-error leakage found on the login page. Public `/portfolio` renders with legacy medium/software content intact (e.g. "Maya", "ZBrush" visible), no runtime errors. Code review of `components/admin/MultiSelect.tsx` confirmed search, chips, remove, full keyboard navigation (Arrow Up/Down, Enter, Escape), an empty state linking to `/admin/metadata?type=…`, and an "Inactive" badge on since-deactivated selections still shown as removable chips — no free-text entry point exists. `/admin/metadata` supports all four groups (Medium, Subject Matter, Software, Tags) per `lib/metadata.ts`'s `METADATA_TYPES`. No authenticated in-browser session was minted for this task (none of this task's rules permit an auth bypass), consistent with Task 2.9/2.10's precedent — static/code-level verification stands in for it. `npm run qa:responsive`: 15/15 passed (two tolerated automatic Chrome relaunches, per Task 2.5's documented recovery behavior — not a failure). `npx tsc --noEmit`: 0 errors. `npm run lint`: 0 errors, 28 warnings (same pre-existing baseline as Task 2.9/2.10, no new ones). `npm run build`: succeeded, all expected routes present including `/admin/metadata`. `git diff --check` and `git status --short` both clean throughout — no code changes were needed, so nothing was fixed under this task's own rules (Section 11: only real, reproducible, small, in-scope Sprint 2 bugs would have been fixed, and none were found). **Vercel production check — a real, confirmed discrepancy, not an access limitation:** the current newest "Ready" production deployment (`portfoliowebsite-9h8vu137d-laeroth.vercel.app`, created before commit `4ea4cb0`, and — per its build manifest, which lists every other current Admin route — also before `a36fbfc`/Task 2.9) does **not** include the `/admin/metadata` route at all, confirmed by a full build-manifest scan (327 build paths, zero containing "metadata", every other Admin route from `admin/3d` through `admin/worldbuilding/new` present) — i.e., production is not running Task 2.9 or Task 2.10's code, so the new Portfolio selector system is not live. (The `/admin/metadata` path itself still 307-redirects to `/admin/login` in production, but this is `proxy.ts` matching the `/admin/:path*` prefix regardless of whether the route exists behind it — confirmed by testing an intentionally nonexistent `/admin/…` path, which redirects identically — so it does not indicate the route is deployed.) Vercel deployment objects carry no Git-source metadata (`meta` is empty), so the deploy trigger mechanism could not be further diagnosed from here. No deploy was triggered, no dashboard setting was changed, and this was checked once, not repeatedly — per this task's own rule, this does not block Task 2.11's (local) acceptance criteria, all of which passed; it is reported as a real, unresolved gap between `main` and production. No new dependency or lockfile change.
- Task 2.12 — Production Sync & Sprint 2 Close: **Complete.** Closed the real gap Task 2.11 found: production had not run any current Git commit since `cc429d1`/Task 2.8 (three commits, two Sprint-2 tasks stale). Root cause confirmed via the Vercel Project API (`/v9/projects/…`): the project has no linked Git integration (`link` object absent from the project record entirely) — every prior "Ready" production deployment was a manually-triggered CLI/dashboard `vercel deploy --prod` run (its embedded `meta.githubCommitSha` reflects whatever commit happened to be checked out locally at that moment, not a push-triggered build), so pushes to `main` were never picked up automatically; this is a real, pre-existing gap in how deployment has been triggered, not a broken connection to fix. `git remote -v` confirms the correct repo (`github.com/Luyome/Portfolio-Website`) matches every deployment's `meta.githubOrg`/`githubRepo`. No dashboard setting, environment variable, domain, or Git connection was changed. GitHub `main` confirmed to already contain `0bb77aa` (`git merge-base --is-ancestor`, no new commit or force-push needed). Ran `vercel deploy --prod --yes` from the existing linked project (`.vercel/project.json`, unchanged) — the safe, project-preserving CLI path — producing a fresh Ready deployment (`dpl_5LCTHXjcSNepxiCV6eXaV1CeHQwX`) confirmed via the Project API to be built from `githubCommitSha: 0bb77aa` (Task 2.11) and aliased to all three production domains including `portfolio-website-eight-xi-79.vercel.app`; its build manifest now includes `/admin/metadata` (absent from the prior deployment). Production verified: `/`, `/portfolio`, `/worldbuilding`, `/about`, `/admin/login` all 200; unauthenticated `/admin`, `/admin/portfolio`, `/admin/metadata` each a single redirect to `/admin/login`, no loop. Using the browser's existing authenticated Admin session (no credentials entered, no session minted): `/admin/metadata` loads its full four-group manager UI directly, and the Portfolio create form (`/admin/portfolio/new`) shows all four `MultiSelect` metadata fields (Medium, Subject Matter, Software, Tags) wired to Admin → Metadata — no content created, changed, or deleted. `npm run qa:responsive` against the new production URL: 15/15 passed (one tolerated automatic Chrome relaunch, per Task 2.5's documented recovery behavior). No code or configuration file changed this task, so type-check/lint/build were not re-run (Section 7); only this deployment action and `docs/08_ROADMAP.md` changed. Remaining real limitation: the Vercel project still has no Git-integration auto-deploy — a future push to `main` will again require a manual `vercel deploy --prod` (or connecting Git integration via the dashboard) unless addressed in a later task; flagged here, not fixed, since connecting Git integration is an architecture change outside this task's bounds.

**Sprint 2 Closure Audit / Hotfix** — A post-close verification pass found and fixed real gaps between what Sprint 2's tasks above reported and the actual code, without adding any feature or starting Sprint 3. Fixed: (1) `readMetadataSelections()` (`lib/portfolio-metadata.ts`) never actually checked `isActive` — an inactive metadata option could be freshly added to any Portfolio item, create or edit, contrary to Task 2.10's intent; now an inactive option is only accepted if it was already selected before the edit (verified with a temporary, non-destructive script covering all 7 required cases — active/inactive on create, kept/newly-added inactive on update, cross-type, unknown, duplicate — deleted after use). (2) Both Blob upload routes returned the raw `error.message` (including Blob/provider detail) to the client on any failure; now a rejected file/policy is a generic 400 and any other failure a generic 500, via a new `UploadPolicyError` distinguishing the two. (3) `MultiSelect` (`components/admin/MultiSelect.tsx`): the first ArrowDown after opening skipped the first option; `aria-selected` was misused for keyboard highlight instead of true selection state; there was no `aria-activedescendant` or per-option `id` — all fixed, `role`/`aria-expanded`/`aria-controls`/chip/search/mouse behavior unchanged. (4) `requiredUrl`/`optionalUrl`/`nullableUrl` (`lib/validation.ts`) accepted any parseable URL scheme, including `javascript:`/`data:`/`file:`; now restricted to `http:`/`https:` like the existing `nullableHttpUrl`, which every current call site (all external/media URL fields — no relative-path field exists) already needed. (5) `/api/download` accepted any `*.public.blob.vercel-storage.com` hostname (any Vercel customer's Blob store); tightened to this project's one confirmed Blob hostname (verified read-only against every content table's stored URLs) plus `redirect: "error"` so a redirect can't hop it off the allow-list. (6) The demo seed (`db/seed.ts`) had no production guard and would duplicate rows on a second run; renamed `db:seed` → `db:seed:demo`, added a `VERCEL_ENV`/`NODE_ENV` production refusal, and an idempotent guard that skips entirely if `services` already has rows. (7) `app/global-error.tsx` used `<h2>` (no real `<h1>`) with no `<main>`; fixed, matching every other error boundary's use of `PageHeader`'s real `<h1>`. (8) `app/robots.ts` disallowed only `/admin/` and `/api/`, leaving the bare `/admin`/`/api` paths technically outside the rule; added. (9) Added the missing `"typecheck": "tsc --noEmit"` script. Investigated and left unchanged: `.github/workflows/deploy.yml` has no evident bug and previously deployed successfully many times but stopped triggering after commit `cc429d1` for a cause not diagnosable without repo-admin/billing access — see `07_TECHNICAL_ARCHITECTURE.md`, section 21 for the full, corrected deployment-mechanism note (this closure task's own push is the first real test of whether it has recovered). Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 warnings), `npm run build` (all routes present), `git diff --check` and `git status --short` clean (only the intended files changed), `npm run qa:responsive` (15/15). Runtime/database verification followed this project's established Vercel-only workflow — verified against the live production URL after deploying, not localhost; local `next dev`/`next start` could not sustain outbound Neon queries for this session's sandboxed shell (confirmed unrelated to this task's changes: identical for all three affected content types, exactly matching the pre-existing static-vs-dynamic route split, and `npm run build`'s own successful static generation already exercises the same query paths). See the final task report for the live-production results. Documentation: corrected Task 2.11's stale `lib/actions/metadata.ts`/`lib/actions/portfolio.ts` function counts; removed the Marketplace bullet from `01_INFORMATION_ARCHITECTURE.md`'s Future Expansion list (it directly conflicted with this roadmap's own Scope Lock) and folded it into that document's explicit-exclusion sentence instead; clarified the Sprint 10 scope-lock wording (section 10) so it can't be read as blocking all future scope forever if Sprint 10, itself optional, never starts; added section 11 recording the still-unresolved Editorial status/Visibility/Featured-boolean documentation conflict as a decision to make before any future content-status implementation, not a change made here.

**Verified Audit Findings (Task 2.1)** — Code review of the Admin route tree, `proxy.ts`, `lib/auth.ts`, `lib/actions/*.ts`, `lib/validation.ts`, the two Blob upload API routes, every Admin form/list/CRUD component, `MapEditor`/`MapPinEditPanel`/`useMapPanZoom`, and the `db/schema.ts` map tables, plus a logged-in browser walkthrough of the Dashboard, Portfolio list/create form, and admin CSS breakpoints.

- **Strong systems to preserve as-is:** route protection (`proxy.ts` gates all of `/admin/*` except `/admin/login`); the Blob upload API routes independently verify the session before issuing an upload token; every dynamic edit route (`portfolio`, `sketches`, `3d`, `games`, `worldbuilding`, `worldbuilding/maps`, `services`) calls `notFound()` on an invalid ID; every delete action is behind a `window.confirm`; the shared `lib/validation.ts` boundary and `safeErrorMessage()` give every mutation consistent, non-leaking server-side validation; the Dashboard's stats and per-section counts are real, database-derived numbers, not decorative; deterministic list ordering is already correct; Admin `noindex,nofollow,nosnippet,noarchive` is set once at `app/admin/layout.tsx` and covers every route including login.
- **Critical findings:** none found.
- **High findings:** (1) Below 820px width, `.adm-sb{display:none}` hides the entire Admin sidebar with no replacement navigation of any kind (no hamburger, no drawer) — an admin on a phone or tablet who lands on any single Admin page cannot navigate to another section without editing the URL by hand. This covers both of the task's required test widths (390px and 768px). Verified directly in `app/globals.css`'s `@media(max-width:820px)` block and confirmed by real headless-Chrome screenshots at both widths in a follow-up pass (see below). (2) Server actions in `lib/actions/*.ts` perform no session check of their own — every mutation (including every `delete...`) is protected only transitively, because Next.js posts a Server Action back to the same `/admin/...` URL that `proxy.ts` already gates. This works today but leaves mutation authorization fully dependent on routing behavior rather than an explicit check at the mutation boundary itself.
- **Medium findings distributed to Task 2.2–2.8:** no empty-state message on any Admin list page when a content type has zero rows (2.5); no field-level validation messages, only one form-level error banner, and no required/optional field indicators beyond native HTML5 `required` (2.4); no "recently added/updated" item list or missing-required-field warnings on the Dashboard, only aggregate counts (2.3); no alt-text field anywhere in the schema for uploaded images (2.6); no upload progress percentage, only a static "Uploading…" label (2.6); no file-size ceiling on either Blob upload route (`maximumSizeInBytes` unset) (2.6 — resolved in 2.7); deleting or replacing a record's image/file never deletes the underlying Blob, so orphaned Blobs accumulate over time (2.6 — still open after 2.7, see its entry); the Map Editor's viewport pan only wires mouse events (`useMapPanZoom`), so touch-dragging the map itself isn't supported on mobile/tablet (pin repositioning already works on touch via Pointer Events) (2.8).
- **Low findings:** the per-row `SaveButton` (`formId` mode, used in table-row forms) disables optimistically for ~400ms instead of tracking real submission status, a narrower double-submit window than the standard nested-form case (2.5).
- **Left to later sprints:** semantic zoom, marker priority/zoom-visibility as real schema fields, the public Map Explorer, new pin layers, and KRUPNI map art direction all remain Sprint 4 scope, not Sprint 2 — confirmed by checking `map_locations` in `db/schema.ts`, which has no priority or zoom-range columns today; adding them is a schema decision that belongs with the Sprint 4 semantic-zoom work, not a Sprint 2 usability pass.
- **Task 2.2's locked starting scope:** ship a mobile/tablet Admin navigation replacement for the sidebar (the confirmed High finding above) alongside the already-planned active-section indication, shared page title/action area, and overflowing table/form field fixes.
- **390px/768px visually confirmed in a follow-up pass this session:** the interactive browser tool still can't shrink its real viewport below desktop width (same Task 1.16 limitation), so this was done instead via a scripted headless Chrome session driven over the DevTools Protocol (`Emulation.setDeviceMetricsOverride`), authenticated by minting a session JWT the same way `lib/auth.ts`'s `createSessionToken()` does — `ADMIN_PASSWORD` was never read or used. Real screenshots plus `scrollWidth`/`clientWidth` measurements were taken for the Dashboard, Portfolio list, Portfolio create form, and Map Editor at both widths. Results: (1) the sidebar-hidden-with-no-replacement finding above is confirmed visually, not just from CSS, on every one of those pages at both widths. (2) The Dashboard itself (stat cards, activity chart) has zero horizontal overflow at either width — a Preserve item. (3) At **390px**, three real, screenshot-visible horizontal overflows were found: the Portfolio list table (390px viewport, 460px content — the fixed-width `.adm-table` column budget, 130px per middle column plus 160px for Actions, leaves the Title column almost no room, so item titles stop being visible at all and Delete is clipped at the edge); the Portfolio create/edit form (390px viewport, 475px content — the `.adm-field-label-row` that pairs a label with its `FieldStyleControls` style pills doesn't wrap, pushing a pill off-screen); and the Map Editor (390px viewport, 716px content — its 6-column Pins table hits the same fixed-column budget problem, more severely). (4) At **768px**, none of the four pages measured any horizontal overflow — but the Map Editor's Pins table, specifically, squeezes its Title/name column to a near-unreadable ~88px there too (five fixed-width columns — Pin Type/Target/Icon/Coordinates/Actions — consume most of the 768px budget even without triggering a scrollbar), a real usability gap distinct from outright overflow. These are now confirmed findings for Task 2.2 (table and form-field overflow is already explicit in its scope) rather than open questions.

**Sprint 2 scope boundaries** — not part of this sprint: Home Page composition, public KRUPNI or Map Explorer, a new Projects system, a Stories or Devlog system, Portfolio public art direction, a multi-language system, public users, comments/likes/bookmarks/community, newsletter, multi-admin or role systems. These remain in their assigned future sprints or out of scope.

**Sprint 3 — Home Page + Home Admin** — *Complete; Tasks 3.1–3.14 complete.* Public Home, in section order: (1) Identity Hero, (2) Selected Work Coverflow, (3) Capabilities/Focus Areas, (4) Skills + Production Stats, (5) KRUPNI Map Preview, (6) Worldbuilding Highlights, (7) Latest Dispatches, (8) Contact + Social. Home Admin: hero management; selection and ordering of Featured Works; Capabilities; Home Skills; automatic Production Stats visibility; map preview; Worldbuilding Highlights; Latest Dispatches; contact/social. Production Stats stay at general categories — 3D Works, 2D Works, Worldbuilding Entries, Game Projects, Stories & Devlogs, Published Entries — without adding overly specific subcategories to Home. Only the minimal creator data actually needed for Home (identity, Home Skills) is created here — the full Creator Profile foundation is Sprint 9 scope.

Content limits for this sprint: Featured Works max 6; Home Skills max 6; Capabilities approx. 3–4; Map Preview markers approx. 3–5; Worldbuilding Highlights max 3; Latest Dispatches approx. 3–4.

Identity note: Tetsunaru is the brand/internet identity; Demir Ünal is the real/professional name. The exact name and brand hierarchy inside the Hero is not locked yet — to be decided visually during implementation, not here.

Sprint 3 tasks:

- Task 3.1 — Existing Home System Audit + Scope Lock: **Complete.**
- Task 3.2 — Home Data Architecture: **Complete.**
- Task 3.3 — Home Admin Architecture: **Complete.**
- Task 3.4 — Identity Hero: **Complete.**
- Task 3.5 — Selected Work Coverflow: **Complete.**
- Task 3.6 — Capabilities + Home Skills: **Complete.**
- Task 3.7 — Automatic Production Stats: **Complete.**
- Task 3.8 — KRUPNI Home Map Preview: **Complete.**
- Task 3.9 — Worldbuilding Highlights: **Complete.**
- Task 3.10 — Latest Dispatches: **Complete.**
- Task 3.11 — Contact + Social: **Complete.**
- Task 3.12 — Responsive + Motion + Accessibility + Performance: **Complete.** — 3.12.1 Public Home Refinement: **Complete** for current Sprint progression; remaining non-blocking final aesthetic polish deferred; Home KRUPNI map integration remains deferred to Sprint 4. 3.12.1B Home Visual Composition Correction: **Superseded / visually insufficient.** 3.12.1C Home Visual Redesign & Acceptance: **Complete.** 3.12.1D Home Centered Composition & Reference Alignment Correction: **Complete.** 3.12.1E Contact Composition Redesign: **Complete.** 3.12.1F Contact Screenshot-Matching Pass (Final): **Complete.** 3.12.1G Contact Sliding-Image Orientation Fix: **Complete.** 3.12.1H Contact Banner Proportion Correction: **Complete.** 3.12.2 Shared Admin Visual Refinement: **Complete.** 3.12.3 Responsive + Motion + Accessibility + Performance Stabilization: **Complete.**
- Task 3.13 — Integration + Browser QA: **Complete.**
- Task 3.14 — Production Deploy + Sprint Close: **Next.**

Sprint 3 scope boundaries (deferred to their assigned sprint, not built here): Full Map Explorer + Map Editor refinement → Sprint 4; Games → Sprint 5; Projects → Sprint 6; Portfolio final redesign → Sprint 7; Stories/Devlogs → Sprint 8; full About/Creator Profile + detailed Skills → Sprint 9; multi-language → Sprint 10; public accounts/community remain out of scope (section 6).

**Contact Composition Redesign + Home Map Deferral (Task 3.12.1E):** A final, focused Home correction — Hero, Header, Selected Work, Capabilities, Skills & Tools, Production Stats, Worldbuilding Highlights, Latest Dispatches, and Footer stayed frozen per the owner's lock; only Contact + Social changed. Restructured Contact from a giant isolated heading plus a left social block and a small right image stack into one centered composition: a top feature panel pairs the (now roughly half-scale) closing statement, supporting copy, and a primary email CTA (`ActionLink`, reused from the Hero) with a dominant crossfading artwork frame in one shared block; a three-module row below (`Contact` compact link list, `Index / Continue` numbered nav, a secondary two-image cluster) shares one grid inside the same `min(1180px,100%)` centered container established in 3.12.1D. The link list dropped its old 78px-row numbered format for a compact 48px-row treatment (Email shown as a two-line label+address entry, socials as single-line label+arrow), matching the owner's reference dashboard's composition logic (one feature band + several smaller balanced modules) translated into the site's dark/restrained-red identity — none of the reference's anime/cyan/rounded-card aesthetic was carried over. Existing three-image crossfade/reduced-motion behavior and all canonical contact/social/nav destinations are unchanged. **Back to Top correction:** per an owner correction issued mid-task, Back to Top was *not* integrated into the Contact composition as the original brief first requested — it is a new, independent `components/BackToTop.tsx`, a conventional fixed bottom-right control (restrained upward-arrow icon, `aria-label="Back to top"`, real `<button>`, keyboard-reachable, fades in past a 480px scroll threshold, `prefers-reduced-motion` skips the smooth-scroll behavior). It renders as a sibling of `.home-page` (not a descendant) in `app/(site)/page.tsx`, specifically because `.home-page` has `overflow:hidden` — verified in-browser that a `position:fixed` descendant of an `overflow:hidden` ancestor is clipped to that ancestor's own box, which would have made the control disappear once scrolled into the Footer; as a page-level sibling it stays anchored for the full scroll range. **Home KRUPNI map:** superseded the earlier restore/preserve instruction per explicit new owner decision — the map integration is deferred to Sprint 4 (Worldbuilding + Map Admin); Home's existing conditional `{homeData.mapPreview && <HomeMapPreview .../>}` render in `app/(site)/page.tsx` was left exactly as-is (no rebuild, no behavior change, canonical map data/models untouched) since it already safely omits itself when no map preview is configured, which satisfies "Home may remain without the KRUPNI map section" without any code change. Verified in-browser (real incremental scroll, not instant jump — `Reveal`'s `framer-motion` `whileInView` does not reliably fire after a programmatic `scrollIntoView` jump straight to a below-the-fold element, a pre-existing characteristic of the shared `Reveal` wrapper affecting every Reveal-wrapped Home section identically, not a Contact-specific defect, and out of this task's scope to change) at a ~2048px effective CSS viewport (this environment's `resize_window` clamps to the host display, per 3.12.1C/D's documented precedent): the feature panel and three-module row both measured centered within ~2px of viewport center, the dominant artwork frame rendered at a real 480px-wide/4:5 frame (versus 3.12.1D's 340px cap), all five configured contact/social links plus the numbered Index/Continue nav and secondary artwork cluster rendered correctly, and Back to Top's fixed position/show-hide-on-scroll/threshold behavior were confirmed via `getBoundingClientRect` and real wheel-scroll interaction (not `window.scrollTo()`, which this automation environment silently no-ops for both direct calls and click-triggered calls — a sandbox artifact, not a code defect; real scroll events update state correctly in both directions). `npm run qa:responsive` (run via PowerShell per the established Git-Bash-leading-slash precedent) against the local dev server: 4/4 passed at exactly 390×844, 768×1024, 1440×900, and 1920×1080 — zero horizontal overflow, zero console errors. `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 pre-existing warnings, no new ones), `npm run build` (all routes present, including `/`), and `git diff --check` all passed; only `app/(site)/page.tsx`, `app/globals.css`, `components/HomeContactSocial.tsx`, and new `components/BackToTop.tsx` changed. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement is next.

**Contact Screenshot-Matching Pass, Final (Task 3.12.1F):** 3.12.1E's Contact composition read as a hand-designed editorial section, not a reproduction of the owner-supplied reference screenshot (a "Miku" fan-dashboard mockup: a bounded, filled top hero panel with character art bleeding past its own edges, followed by a three-column module row — two square artwork thumbnails + a full-width button, a dominant middle list of pill-style rows with an overlapping circular avatar cluster, and a right column of stacked icon/title/description nav cards). Rebuilt `HomeContactSocial` to reproduce that literal geometry with real Tetsunaru content and the site's dark/red identity (none of the reference's cyan/anime skin carried over): the feature block is now `.hcs-panel`, a bounded rounded card (border + gradient fill) whose crossfading artwork frame overflows past the panel's own top edge via a negative-inset `position:absolute` frame, with two short decorative marks under the CTA echoing the reference's tab-indicator lines. Below it, `.hcs-modules` is an explicit `.85fr 1.3fr .95fr` grid (Contact deliberately the widest column, matching the reference's dominant middle list) in reference left-to-right order: **Artwork** (new `hcs-artwork-grid`, two square linked thumbnails with a bottom-gradient title caption, plus a full-width `View More` button pinned to the column's bottom via `margin-top:auto`); **Contact** (the existing link list restyled as bordered pill rows with the primary link visually highlighted, plus a new absolutely-positioned `hcs-avatar-cluster` of two overlapping circular crops bleeding into the column's right edge, `pointer-events:none` so they never block the links underneath); **Index/Continue** (the existing three nav shortcuts restyled as `hcs-navcard` rows — a circular numbered badge + title + one-line description each, matching the reference's icon/title/description card format, using real static nav copy, not fabricated data). New `artworks` prop (`HomeContactSocial.tsx`) feeds the Artwork column from real Home data — `app/(site)/page.tsx` now passes `[...featuredWorks, ...homeData.worldbuildingHighlights]` (both already-fetched, real, titled, hrefed content) filtered to items with an image, since the current dev dataset's Featured Works are all placeholder-art (no image yet) while Worldbuilding Highlights do have real images; featured, image-bearing work will naturally take priority over worldbuilding once Featured Works get real artwork through the existing Admin flow — no new query, no invented content. **Environment note, verified and worth recording for future sessions:** this task's browser automation tab is a background tab inside the owner's own live Chrome session (confirmed by capturing the real desktop via a temporary read-only screenshot script, not by inspecting any personal browsing content beyond what was needed to diagnose a blank-screenshot bug), so screenshots taken while the owner is actively using another tab return blank (Chrome deprioritizes painting non-visible tabs; `document.visibilityState` was confirmed `"hidden"` during the blanked attempts even though `hasFocus()` stayed `true` and all DOM/JS calls kept working normally). Stopped and asked the owner before forcing tab focus (per this repo's action-permission rules); with explicit go-ahead, obtained one clear, focused 1440-width screenshot of the rebuilt Contact top panel and the start of the module row (heading, copy, CTA, decorative marks, the bleeding artwork frame, the Contact column's highlighted Email pill, and the start of the Index/Continue column all visually confirmed present and positioned as designed) before focus reverted to the owner's own tab again; further repeated attempts (resize/refocus/`scroll_to`-by-element-ref, `window.scrollTo()` itself confirmed still a no-op per 3.12.1E's precedent) could not reliably sustain a second full-page shot without repeatedly stealing the owner's active tab, so continuous side-by-side re-comparison was intentionally stopped rather than kept forcing tab focus. That one screenshot's structure was cross-checked against real DOM measurements (`getBoundingClientRect` on `.hcs-panel`/`.hcs-modules`/each column, server-rendered HTML via `curl`) confirming: zero horizontal overflow (`scrollWidth === clientWidth` at the ~2048px effective viewport this environment's `resize_window` clamps to, per 3.12.1C–E precedent), the three columns measured 259px/396px/290px wide (Contact clearly dominant, matching the reference proportion), both Artwork thumbnails and their real title captions server-rendered once the `artworks` source above was broadened, exactly 3 nav cards and 5 link rows present, and the avatar cluster rendered. `npm run qa:responsive` passed 20/20 (two tolerated automatic Chrome relaunches, consistent with Task 2.5's documented recovery behavior) at exactly 390×844, 768×1024, 1440×900, and 1920×1080 across `/`, `/portfolio`, `/worldbuilding`, `/about`, `/admin/login` — zero horizontal overflow, zero console errors on any route/viewport, confirming the new module-row breakpoints (`.hcs-modules` collapses to two columns with the nav row spanning full width under 980px, then one column under 520px) don't regress. `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 pre-existing warnings, no new ones), `npm run build` (all routes present, including `/`), and `git diff --check` all passed (only line-ending warnings, no real whitespace errors); only `app/(site)/page.tsx`, `app/globals.css`, and `components/HomeContactSocial.tsx` changed — `components/BackToTop.tsx` and its fixed, page-level-sibling placement from 3.12.1E were confirmed untouched and out of this pass's scope, per the task's own explicit exception. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement remains next; Home KRUPNI map integration remains deferred to Sprint 4.

**Contact Sliding-Image Orientation Fix (Task 3.12.1G):** A real owner-reported bug in 3.12.1F's rebuilt feature frame, found from the owner's own screenshot of the live page (not the reference screenshot): `.hcs-frame-main` used a portrait `aspect-ratio:3/4` and a broken `position:absolute;inset:clamp(-40px,-4vw,-56px) 0 0 auto` — at its resolved 420×560px box this overflowed roughly 200px below the feature panel and visually overlapped the Artwork/Contact/Index headings of the module row underneath, and the owner separately required every rotating Contact image to be **horizontal/landscape framed**, never portrait, and to change via a horizontal **slide** rather than the existing opacity crossfade ("3 görselin YATAY olarak kaydığı… değişen görseller YATAY olmak zorunda"). Fixed both in `HomeContactSocial.tsx`/`globals.css`: `.hcs-frame-main` is now `aspect-ratio:3/2` (landscape) and a normal in-flow, `position:relative` element (`margin-left:auto` to sit against the panel's right edge, a modest `clamp(-52px,-4.6vw,-28px)` `margin-top` for a restrained bleed above the panel — not the old, broken 200px overflow) so it can no longer intrude into the module row below; the rotating images (still the same `images` prop/interval/pause-on-hover-or-focus/reduced-motion logic from 3.12.1E, unchanged) now animate via a new `slideOffset()` shortest-path helper driving `transform: translateX(±100%…)` with a `transition: transform .9s cubic-bezier(.4,0,.2,1)` on the `<img>`s, instead of the old `opacity` crossfade — a real horizontal slide, not a fade — while `prefers-reduced-motion` (already listing `.hcs-frame-main img` in its `transition:none` block from 3.12.1E) makes the same state change instant with no motion, unchanged pattern. The mobile (≤820px) override was simplified to match (landscape `aspect-ratio:3/2`, centered, no more portrait `4/5` fallback). Verified via `getBoundingClientRect` at the ~2048px effective viewport (this environment's `resize_window` clamp, per 3.12.1C–F precedent): the frame now measures a real 439×293px box (≈3:2, landscape confirmed), its bottom edge sits at 689px while the module row starts at 846px — a clean 157px gap, zero overlap (previously overlapping) — and `document.documentElement.scrollWidth === clientWidth` (zero horizontal overflow) held throughout. Browser-automation screenshots were attempted again but hit the same shared-tab-focus contention documented in 3.12.1F (the owner's own live Chrome tab repeatedly reclaiming foreground); rather than keep forcing focus, this pass relied on the DOM measurements above plus the owner's own bug report/screenshot as the acceptance signal, consistent with 3.12.1F's precedent for this specific, previously-flagged environment limit. `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 pre-existing warnings, no new ones), `npm run build` (all routes present, including `/`), `npm run qa:responsive` (20/20 passed, two tolerated automatic Chrome relaunches per Task 2.5's precedent, zero overflow/console errors at 390×844/768×1024/1440×900/1920×1080 across all 5 routes), and `git diff --check` all passed; only `app/globals.css` and `components/HomeContactSocial.tsx` changed. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement remains next.

**Contact Banner Proportion Correction (Task 3.12.1G's DOM-measurement acceptance turned out to be insufficient — Task 3.12.1H):** 3.12.1G's DOM checks (frame aspect ratio, zero overlap, zero overflow) were all real and correct, but the owner directly compared their own screenshot of the live result against the reference again and found the overall *panel* silhouette still didn't read as the reference's short, wide banner — the top feature panel was still a roughly square/tall block (~2:1 width:height) next to the reference's very short, wide one (~3.3:1), because the heading wrapped to 3 lines at a large size and the panel kept its original generous padding, so fixing only the image's own aspect ratio (3.12.1G) wasn't enough to fix the *panel's* silhouette. Tightened `.hcs-panel`/`.hcs-feature`/`.hcs-frame-main` together in `globals.css` (no `HomeContactSocial.tsx` change needed this time): gave the text column relatively more width (`grid-template-columns` `1.15fr`/`.85fr`, was even `1fr`/`.92fr`) so the same heading text now wraps to 2 lines instead of 3; reduced the heading's `font-size` clamp (`1.5–2.15rem`, was `1.9–3.2rem`) and tightened its `line-height` to `1.02`; reduced the copy/CTA-row/decorative-marks vertical spacing and the panel's own padding (`clamp(22–34px)`, was `32–56px`); and changed the sliding frame from `aspect-ratio:3/2` to a shorter `2/1` at a smaller `max-width` clamp, matching the reference's flatter banner-style crop instead of merely "any landscape ratio." Also tightened the lower module row to match the reference's denser list feel: `.hcs-links a` `min-height` `52→46px` and tighter row/kicker margins; `.hcs-navcard` padding `12→10px`; `.hcs-modules` gap and kicker margins reduced — closing the gap where the Artwork column's thumbnails sat far above an empty stretch before its `View More` button (caused by the Contact column's taller list dictating the shared row height) by shrinking that list instead of growing the thumbnails. Verified via `getBoundingClientRect` at the ~2048px effective viewport (same environment clamp as every prior 3.12.1 pass): the panel now measures a real 1015×305px box, a **3.32:1** width:height ratio — matching the reference's own ≈3.3:1 banner silhouette to within rounding — and the sliding frame measures 397×198px (**2.00:1**, confirmed landscape). `document.documentElement.scrollWidth === clientWidth` held (zero horizontal overflow). Unlike 3.12.1E–G, this pass got a real, focused browser screenshot of the local dev result (the owner's live Chrome tab was on a different site at the moment of capture, so the automation tab held focus for one clean shot) showing the corrected panel and full module row side by side with no overlap and the heading on 2 lines — visually confirming the DOM ratios above, not just inferring from them. `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 pre-existing warnings, no new ones), `npm run build` (all routes present, including `/`), `npm run qa:responsive` (20/20 passed, two tolerated automatic Chrome relaunches per Task 2.5's precedent, zero overflow/console errors at 390×844/768×1024/1440×900/1920×1080 across all 5 routes), and `git diff --check` all passed; only `app/globals.css` changed. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement remains next.

**Shared Admin Visual Refinement (Task 3.12.2):** A browser-first pass across the existing owner-only Admin — presentation/layout only, no CRUD, data, or Sprint 4+ domain architecture changed. Audited representative Admin pages (Dashboard, Home, Portfolio list/create/edit, Worldbuilding, Games, Settings, Metadata) at 1440×900 in the owner's authenticated local session (the owner logged in manually after the harness declined an in-browser session-minting workaround) and confirmed the reported problem directly: narrow ~640px forms and ~832px-wide tables stranded in a ~960px-capped `.adm-wrap`, leaving hundreds of pixels of dead space on wide viewports, plus a real, reproducible bug — the Metadata table's 3-action cell (Save/Edit/Delete) clipped to "DELE" at the old fixed 160px last-column width. Refined shared `globals.css` tokens used by every Admin page: `.adm-wrap` max-width 960→1280px (1600px for the Dashboard's already-full-bleed grid) with `margin-inline:auto` so extra width centers instead of hugging the sidebar edge; `.adm-form`/`.adm-edit-solo` 640→860px with `margin-inline:auto`; form control height/padding/focus ring strengthened (`min-height:46px`, `box-shadow` focus ring); `.adm-table` last-column 160→210px (fixes the Metadata clipping bug); `.adm-btn`/`.adm-form-cancel` given equal 46px min-height and a bordered secondary treatment so Save/Cancel read as primary/secondary; `.adm-actions button.danger` dimmed to `opacity:.75` so Delete doesn't visually compete with Save. Two new shared primitives: `components/admin/AdminPageHeader.tsx` (title + description + right-aligned primary action, one `<h1>` per page) replacing the repeated bare `.adm-title`/`.adm-sub`/action sequence on all 31 Admin page files except `/admin/login` (intentionally out of scope — not an owner content-management screen); `components/admin/AdminSection.tsx` (bordered field-group panel) plus a new `.adm-form-row` two-column grid class, applied to group related fields ("Basic Information", "Media & Display", etc.) and pair short fields (Title | Category, Year | Display Date) in `PortfolioForm`, `WorldbuildingForm`, `GameForm`, `Model3DForm`, `SketchForm`, `ServiceForm`, `HomeHeroForm`, and the Settings page — long fields (description/excerpt/content editors) stayed full-width per the task's explicit rule. Worldbuilding/Map Admin (`MapEditor`, Map Pins) received only the header/width/table CSS shared by every page, per the Sprint 4 scope lock — no Map Editor, pin-system, or Explorer change. Verified in the owner's real authenticated session: Dashboard, Home (all 8 sections), Portfolio (list + create + edit with real data), Worldbuilding (list + create), Games (list + edit with real data), Settings, and Metadata all render the new shared system correctly with zero DOM-measured horizontal overflow (`scrollWidth===clientWidth` at the ~2048px effective viewport this environment's `resize_window` clamps to, per every prior 3.12.1 pass's precedent); a non-destructive Portfolio edit-and-resave round-trip confirmed Save still writes correctly and no data changed (title/row count unchanged before and after). True 390×844/768×1024 device-viewport testing of authenticated Admin pages could not be performed this pass — this environment's `resize_window` cannot shrink below the host display for any page, and the CDP-script-with-a-minted-session-JWT workaround every earlier Sprint-2 task used for this exact limitation was specifically declined this session in favor of the owner logging in manually; the new responsive CSS (`.adm-page-header`, `.adm-form-row`, table wrap) reuses the same breakpoints and mechanisms (820px sidebar-to-drawer swap, `.adm-table-wrap` horizontal scroll) already verified at those widths in Task 2.2, so this is a real, reported gap, not a silent skip. `npm run qa:responsive`: 20/20 passed across `/`, `/portfolio`, `/worldbuilding`, `/about`, `/admin/login` at 390×844/768×1024/1440×900/1920×1080 (run across two full passes plus one isolated PowerShell run to work around this machine's known intermittent headless-Chrome crashes, per Task 2.5's precedent — zero overflow, zero console errors on every route/viewport including `/admin/login` at 1920×1080, confirming the shared CSS changes don't regress the public site or login page). `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 pre-existing warnings, no new ones), `npm run build` (all routes present), and `git diff --check`/`git status --short` (only intended files changed; `AGENTS.md`/`.agents/` left untouched) all passed. Task 3.12 remains in progress; 3.12.3 Responsive + Motion + Accessibility + Performance Stabilization is next.

**Responsive + Motion + Accessibility + Performance Stabilization (Task 3.12.3):** A stabilization-only pass over the current public Home and shared Admin, per the task's explicit "fix only real problems, do not redesign" scope. Audited responsive behavior at 390×844, 768×1024, 1440×900, and 1920×1080; existing motion (`Reveal`, `HomeContactSocial`'s slide rotation, `BackToTop`); reduced-motion handling; focus/keyboard behavior; and timer/listener cleanup across the components touched by 3.12.1–3.12.2. Found no real regressions: `BackToTop` already correctly skips smooth-scroll under `prefers-reduced-motion`, syncs `aria-hidden`/`tabIndex` with visibility, and uses a passive scroll listener with cleanup; `Reveal` and `HomeContactSocial`'s rotation interval already skip their animation/timer entirely under reduced motion (`usePrefersReducedMotion`, a hydration-safe `useSyncExternalStore` hook with the matching CSS override applying before first paint) with correct `useEffect` cleanup; every interactive Home/Admin control audited has a visible `:focus-visible` outline; `globals.css`'s reduced-motion rules (global + section-specific `hcs-`/`hwh-`/`hld-`/`hmp-`/`sw-` blocks) already neutralize the relevant transitions/animations without the disallowed blanket `*` rule. No code change was required or made — a zero-diff, stable-implementation outcome, as the task's own acceptance criteria anticipate. Validation: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 pre-existing warnings, no new ones), `npm run build` (all 41 routes present, including `/` and `/admin/login`), `npm run qa:responsive` against a production `next start` build (20/20 passed — one tolerated automatic Chrome relaunch per Task 2.5's precedent — zero horizontal overflow, zero console errors at all four required viewports across `/`, `/portfolio`, `/worldbuilding`, `/about`, `/admin/login`), and `git diff --check`/`git status --short` (only the pre-existing, out-of-scope `AGENTS.md`/`.agents/` working-tree state remained, untouched by this task). Task 3.12 is now complete; Task 3.13 Integration + Browser QA is next.

**Home Section Navigator + Integration QA (Task 3.13):** Added a fixed, Home-only vertical scrollspy rail, `components/HomeSectionNav.tsx`, rendered as a page-level sibling of `.home-page` (same pattern as `BackToTop`, avoiding its `overflow:hidden` clipping). Content is server-derived in `app/(site)/page.tsx` (`navSections`) from the same conditions that already gate each section's render (`visibleProductionStats.length`, `homeData.worldbuildingHighlights.length`, `homeData.latestDispatches.length`, `contactSocialLinks.length`), so the rail never links to an omitted section; the KRUPNI Map is excluded unconditionally per the Sprint 4 deferral regardless of `homeData.mapPreview`, per explicit instruction. Added stable section `id`s where missing (`#home-hero`, `#worldbuilding-highlights`, `#dispatches`, `#contact` — `#selected-work`/`#capabilities`/`#skills`/`#stats` already existed) with no layout change. Tracking uses one shared `IntersectionObserver` (multi-threshold) rather than a scroll listener; clicking an item calls `scrollIntoView` with `prefers-reduced-motion` swapping `smooth`→`auto`, matching `BackToTop`'s established pattern. Visual language is minimal by design: small neutral dots (`.hsn-dot`), active state uses the existing `--red`/scale treatment, hover/focus reveals the section label via a `max-width` transition — no panel, no glass, no oversized labels. Rail is `display:none` below 1180px (pure CSS media query, verified both via computed style and by reading the stylesheet's own `CSSMediaRule` at runtime) so it doesn't compete for room at 390×844 or squeeze Home content; visible and vertically centered at wider widths. Browser QA (real dev/production server, not build-only): at the ~2048×1022 effective viewport this environment's `resize_window` clamps to (same documented limit as every 3.12.1–3.12.3 pass), the rail rendered exactly 7 items matching the current dataset's actually-visible sections (Hero, Selected Work, Capabilities, Skills, Production Stats, Worldbuilding, Contact — Dispatches correctly omitted, current dev data has none configured), measured vertically centered with zero rect-overlap against `.back-to-top`, and a real (non-programmatic) wheel-scroll confirmed the active item updates correctly via the IntersectionObserver as different sections cross the viewport. Programmatic (`.click()`-triggered) `scrollIntoView` was a no-op in this sandbox — the same documented `window.scrollTo()`/click-triggered-scroll artifact recorded in Task 3.12.1E/F, not a defect in the shipped code, which uses the identical, already-verified `BackToTop` scroll pattern. True 390px/768px device-viewport rendering could not be captured visually for the same recurring `resize_window` host-clamp reason as every prior Sprint-3 pass; the hidden/visible states at those widths rest on the deterministic CSS media query itself plus `npm run qa:responsive`'s real, separate-process headless-Chrome run at exactly those widths (zero overflow, zero console errors). No unrelated Home/Admin visual change was made; Hero, Selected Work, Capabilities, Skills, Production Stats, Contact, Header, Footer, and Admin stayed frozen. `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, the same 28 pre-existing warnings, no new ones), `npm run build` (all 41 routes present), `npm run qa:responsive` against a production `next start` build (20/20 passed at 390×844/768×1024/1440×900/1920×1080 across `/`, `/portfolio`, `/worldbuilding`, `/about`, `/admin/login` — zero overflow, zero console errors), and `git diff --check`/`git status --short` (only intended files changed; `AGENTS.md`/`.agents/` left untouched) all passed. Task 3.13 is now complete; Task 3.14 Production Deploy + Sprint Close is next.

**Verified Home audit / scope lock (Task 3.1):** Preserve and extend the existing server-rendered Home route, Site Settings identity/contact/social data, Hero media/buttons, ordered manual showcase foundation, Services/Focus Areas CRUD, shared media/motion/reduced-motion/layout/metadata systems, and existing Portfolio/2D/3D/Games/Worldbuilding/Map records. Sprint 3 must add only Home-scoped Skills, automatic broad-stat visibility, content-linked ordered Featured Works (max 6), Map Preview selection (approx. 3–5 markers), ordered Worldbuilding Highlights (max 3), and Latest Dispatches configuration (approx. 3–4); it must not expand the deferred destination systems. The global editorial status/visibility/Featured model remains unresolved and is not required for Home-specific ordered curation, but the owner must define what counts as a “Published Entry” (or approve hiding that stat) before Task 3.2 can finalize that automatic count because current content tables have no publication field.

**Home data architecture (Task 3.2):** Added Home-scoped ordered content references for Featured Works (max 6), Worldbuilding Highlights (max 3), and Latest Dispatches (max 4), with real foreign keys to existing Portfolio, Sketch, 3D, Worldbuilding, and Game records plus database checks enforcing one target per row and Worldbuilding-only highlights. Added Home Skills (max 6 in the application boundary), Services-based Capability visibility/order (max 4), broad production-stat visibility settings, and a singleton KRUPNI map preview with ordered pins tied to existing map locations (max 5). Unsupported Stories & Devlogs and Published Entries stats remain explicitly unavailable/hidden until their source models exist or the pending publication definition is resolved. `lib/home-data.ts` is the consolidated read/write data boundary for later Home tasks; no Task 3.3 Admin UI was started. Additive migration `0020_flashy_thunderbolts` was applied only to the verified Neon `development` branch. Development integrity checks, 4 targeted tests, type-check, lint (0 errors; same 28 existing warnings), production build, and `git diff --check` passed.

**Home Admin architecture (Task 3.3):** Extended `/admin/home` as the single management surface for all eight locked Home sections while preserving the legacy Hero and Showcase controls still consumed publicly. Reusable ordered-curation controls select only existing records, expose limits and visibility, and provide keyboard/touch Up/Down and removal actions; Services, Map Admin, and Site Settings remain their canonical source editors. New actions authenticate before parsing, return safe validation feedback, and reuse Task 3.2's relationship and limit enforcement. Stories & Devlogs and Published Entries remain unavailable; the global status/publication definition is still unresolved.

**Identity Hero (Task 3.4):** Reworked the public Hero into a balanced, media-led composition using the existing Site Settings identity fields, Hero copy/buttons, and Hero slide records. Tetsunaru is the display identity, paired directly with the professional name and role; localized shading keeps artwork visible, while one primary, one secondary, and any further quiet text actions preserve configured destinations without competing with the identity. The existing carousel retains configurable media, captions, links, restrained crossfades, manual controls, and reduced-motion behavior, with an accessible pause control and 44px targets. No schema, dependency, or later Home section was added.

**Selected Work Coverflow (Task 3.5):** Added the media-led, user-controlled Selected Work section directly after the Identity Hero using the existing Home-specific ordered curation (maximum 6, separate from any global Featured/status model), real content metadata, and currently valid destination routes; invalid/deleted references continue to be omitted by the data boundary and the legacy Showcase no longer duplicates it publicly. Owner-approved Home direction is sans-serif, with primary positioning centered on Game Designer + Worldbuilding while engine/software remains supporting metadata. The Home narrative proceeds from identity (“Who I am”) to work (“What I made”) and continues through capabilities, production range, worldbuilding, current work, and contact in assigned tasks. Full Header/Nav visual refinement remains deferred to Task 3.12.

**Task 3.5 final Hero refinement:** Locked the desktop Hero as a full first-viewport cinematic opening with a centered, wider composition and an approximately 10% larger sans-serif information scale; Selected Work begins after scroll. Tablet and mobile retain independent scaling.

**Capabilities + Home Skills (Task 3.6):** Finalized the Home sequence as Hero → Selected Work → Capabilities → Home Skills. Selected Work retains the owner-controlled maximum of 6 while the public Coverflow exposes only left/active/right positions; remaining works stay off-stage, side selection centers that work, Previous/Next wrap circularly, and calm approximately five-second autoplay pauses for hover/focus and is disabled with spatial motion under reduced-motion. Non-persistent presentation placeholders fill uncurated slots and disappear automatically as real owner curation reaches six; final artwork replacement remains late-stage content work. Removed the conflicting three-category Home portal presentation. Refined the existing Services-backed editorial Capabilities section to be approximately 5% more compact with stronger description readability, and preserved the minimal ordered Home Skills model (0–6 entries, no proficiency scoring). Production Stats remain Task 3.7 and are not rendered. Header/Nav and shared Admin refinement remain Task 3.12; domain-specific Admin refinement remains Sprints 4–9.

**Task 3.6 detail-route UX fix:** Real Selected Work items resolve to their exact existing content-detail modal through canonical item deep links; the active artwork, title, and View More share that destination, while side cards remain selection controls and placeholders remain non-navigable.

**Automatic Production Stats (Task 3.7):** Integrated a compact editorial stats row with the existing Home Skills area. Counts are automatic aggregate queries over the canonical 3D, Sketch/2D, Worldbuilding, and Game tables and respect the existing Home visibility configuration and canonical category order. The public section omits itself when no stats are enabled and supports partial configurations; unsupported Stories & Devlogs and Published Entries remain unavailable and cannot render a fake zero. No manual value field, schema change, publication model, or future content model was added.

**KRUPNI Home Map Preview (Task 3.8):** Added a compact, atmospheric map section after Skills + Production Stats using the existing Home visibility configuration, canonical map artwork, and ordered selection of up to five canonical pins. The server-rendered preview provides accessible marker and Worldbuilding links without loading the full Explorer client bundle, renders partial marker sets, and omits itself safely for hidden, deleted, mismatched, or missing maps. No schema, duplicate map data, new dependency, or Sprint 4 Explorer feature was added.

**Worldbuilding Highlights (Task 3.9):** Added a compact, media-first KRUPNI editorial section after the optional Home map preview. It respects the existing ordered Home curation up to three records and links every item to its canonical Worldbuilding detail state. When owner curation is empty, the public read uses the first three real canonical Worldbuilding records in stable source order as a non-persistent preview fallback; partial curation remains authoritative, deleted or invalid references fail safely, missing media uses a visual-only archive treatment, and zero legitimate records omit the section. No schema, content model, persisted fallback, or invented lore was added.

**Latest Dispatches (Task 3.10):** Added a compact editorial list after Worldbuilding Highlights using only Home-curated canonical Portfolio, Sketch, 3D, Worldbuilding, and Game records. Entries preserve their real content type, title, summary, creation timestamp, and exact existing detail route; the public boundary rejects invalid records, orders deterministically newest-first, caps output at four, and omits the section when no legitimate curation exists. No fallback records, schema changes, publication system, or Sprint 8 Stories/Lore/Devlog models were added.

**Contact + Social (Task 3.11):** Replaced the legacy Home contact CTA with a concise final editorial section sourced exclusively from canonical Site Settings contact and social fields. Configured email and HTTP(S) social destinations retain their exact canonical targets, invalid or empty values are omitted, partial configurations render without placeholders, and a fully empty configuration omits the section. Existing optional Home contact artwork remains an atmospheric background treatment, while the genuine global Footer remains separate and unchanged. No contact form, duplicate profile/contact model, schema change, fabricated personal data, or Task 3.12 polish was added.

**Public Home Refinement (Task 3.12.1):** Refined the public Header, Home rhythm, Selected Work hierarchy, Skills/Stats presentation, Highlights/Dispatches integration, and Contact closing scene. Replaced the KRUPNI preview treatment with a large bounded zoom/pan map whose maximum five Home pins resolve only legitimate continent submaps and open their exact canonical map state; no nested Home marker layer was introduced. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement is next.

**Home Visual Composition Correction (Task 3.12.1B):** Tightened Selected Work into one connected Coverflow composition, consolidated sparse Skills and canonical Production Stats into a compact production-profile region, enlarged the KRUPNI map as the section's primary media object, and normalized the intervening Home rhythm. Existing interactions, canonical data/routes, and frozen Home/Admin areas remain unchanged. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement is next. **Superseded by Task 3.12.1C** — the owner reviewed the result in-browser and rejected it as visually insufficient (Skills and Production Stats were still combined side-by-side in one shared row rather than being independent sections, and the overall change read as minor spacing polish rather than a structural redesign).

**Home Visual Redesign & Acceptance (Task 3.12.1C):** A structural, browser-verified visual correction pass, not a spacing-only pass — Hero and Header stayed frozen per the owner's lock. Split the previously combined Skills + Production Stats block (Task 3.12.1B) into two independent, full-width Home sections in the required sequence (Capabilities → Skills & Tools → Production Stats → KRUPNI), each individually spacing-tuned rather than sharing one global padding value. Skills & Tools is a full redesign: compact bordered tiles (`hsk-` prefix — `.sk-` was already the Sketches archive grid's class, so Skills uses its own namespace to avoid a real collision) pairing a canonical software logo with the skill name, no proficiency percentages/meters/stars per the owner's explicit rule; logos reuse the existing `knownSoftwareIcons`/simpleicons.org pattern from Task 3.7 (added Blender and Maya alongside the existing Unreal Engine entry — the only three of this project's configured Software metadata options with a real Simple Icons match, verified by request before adding); skills without a matching logo fall back to a two-letter typographic glyph instead of an invented icon; a single configured skill renders as one compact tile, not a stretched near-empty section; the zero-skills state is an unchanged, compact "being curated" line. Production Stats is now its own compact section with a horizontal flex row (not a stretched equal-width grid), so one or two visible stats stay snug instead of reserving a four-column empty layout. Selected Work: reduced section/stage/footer padding and removed the footer's separating margin+border so title, summary, View More, and Prev/Next/pagination read as one connected unit directly under the active artwork, per the owner's structural request — the approved Coverflow logic (max 6, active-center, circular nav, autoplay/pause, reduced-motion, canonical routing) is unchanged. Contact + Social is a full redesign of the closing scene: a bounded three-image composition (one crossfading dominant frame + two static offset secondary frames, real project/artwork media, `prefers-reduced-motion` static) replaces the previous full-bleed low-opacity background wash; the closing headline is reduced roughly by half versus the previous pass so it no longer reads as a second Hero; quick navigation is a numbered "Index / Continue" list (Work / Worldbuilding / About·CV) visually distinct from a separate "Back to Top" utility control; all existing canonical contact/social links, destinations, and the Footer remain unchanged. Capabilities and the KRUPNI map preview received only outer-spacing/rhythm tuning, per scope — no logic, layout family, or map architecture change. Verified with the actual local Home page in-browser (not screenshots-only, not build-success-only): Hero, Selected Work (0/1-item and default states), Capabilities, Skills & Tools (0-skill, 1-skill, and 5-skill states — the 1- and 5-skill states were checked using temporary, non-destructive `home_skills` rows inserted and then deleted after inspection, matching this project's established temporary-QA-script precedent), and Production Stats were each visually confirmed at 1440×900 before/after; Contact + Social's redesigned three-frame composition, link list, and numbered navigation were confirmed via real rendered-DOM geometry and content (`getBoundingClientRect` on all three frames, full `innerText` of the section) after the interactive browser tool's screenshot capture became persistently unresponsive for that one section deep in the page (a pre-existing, documented limitation of this environment's automated browser under sustained use — see Task 2.5 — not a defect in the changed code). `npm run qa:responsive` against the running dev server independently re-confirmed the full Home page (including the redesigned sections) at all four required viewports (390×844, 768×1024, 1440×900, 1920×1080): 20/20 passed, zero horizontal overflow, zero console errors. No schema, dependency, canonical routing, Hero, Header, Coverflow interaction, or map-architecture change was made. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement is next.

**Home Centered Composition & Reference Alignment Correction (Task 3.12.1D):** The owner reviewed 3.12.1C live and rejected the composition again — not for spacing, but because Skills, Production Stats, and Contact still read as **left-anchored**: each section's inner container was itself centered on the page, but the actual content group inside it (the flex-wrapped tile/stat row, the two-column Contact grid) defaulted to flex/grid's normal start-alignment, so the visible group sat against the container's left edge with the unused width trailing off to the right. Measured directly in-browser before the fix (`getBoundingClientRect` on the real tile/stat groups at a ~2048px CSS viewport): the Skills tile group's visual center sat at x≈814 against a viewport center of 1024 (210px left of center); Production Stats sat at x≈637 (387px left of center). Fixed with `justify-content:center` on `.hsk-grid`/`.st-row` and centered, stacked (not left-title/right-description split) `.hsk-heading`/`.st-heading` blocks — re-measured after the fix at x≈1023 for both groups, matching the 1024 viewport center. Contact + Social was recomposed, not just re-centered: the heading is now one centered block above the fold of the section (previously paired left-heading/right-image), followed by a single centered `.hcs-inner` (narrowed to `min(1180px,100%)`, distinct from the wider `1512px` shared by the editorial/showcase sections) containing a balanced two-column module — contact links + "Index/Continue" nav on one side, an enlarged three-frame image cluster (main frame widened from 300px to 340px max-width) + "Back to Top" on the other — so the whole closing scene reads as one deliberately centered composition instead of left-aligned text plus detached imagery; the existing three-image crossfade/reduced-motion behavior, canonical contact/social links, and shortcut destinations are unchanged. Selected Work and the KRUPNI map preview were inspected the same way and found already correctly centered (Selected Work's active card and footer both measured within 1px of viewport center; `.hmp-interactive` already used `margin-inline:auto`) — per the task's own scope, neither was touched. Capabilities was left as editorial left/right composition, per the task's explicit Type A/Type B distinction (editorial sections keep internal alignment; showcase sections — Selected Work, Skills, Stats, KRUPNI, Contact — get the centered-composition treatment). Verified in-browser at a ~2048×1022 CSS viewport (this environment's `resize_window` clamps to the host display and could not hit exactly 1440×900, per Task 3.12.1C's precedent) using temporary, non-destructive `home_skills` rows (6, matching production's real configured skills, then 3 for a mobile-width recheck) inserted and deleted after inspection: Skills and Production Stats' centering was confirmed both by direct screenshot (an unmistakably centered tile row and stat strip, symmetric side margins) and by geometry; Contact's centering was confirmed by geometry (`.hcs-inner`/`.hcs-heading`/`.hcs-body` all centered within 1px of viewport center) after the interactive browser tool's screenshot capture again became unresponsive that deep in the page (the same pre-existing environment limitation noted in 3.12.1C and Task 2.5, not a defect in the changed code). `npm run qa:responsive`: 20/20 passed (390×844/768×1024/1440×900/1920×1080 × 5 routes), zero horizontal overflow, zero console errors, including two tolerated automatic Chrome relaunches per the documented Task 2.5 precedent. No schema, dependency, Hero, Header, Coverflow logic, or map-architecture change was made. Task 3.12 remains in progress; 3.12.2 Shared Admin Visual Refinement is next.

Sprint 2 final baseline commit for this planning pass: `bed4b9c7229518cac2357d6a1f7b7e69da6fdf49`.

**Sprint 4 — Worldbuilding + Map** — **Complete.** Tasks 4.1–4.10 are complete; Sprint 5 — Game Design + Games Admin is next.

- 4.1 — Worldbuilding + Map Audit & Scope Lock
- 4.2 — Worldbuilding Data + Taxonomy + Relations
- 4.3 — Public Worldbuilding Visual Discovery Grid + Filters (media-first / ArtStation-style discovery grid adapted to the site's dark premium identity)
- 4.4 — Shared Worldbuilding Content Detail Shell (large artwork/media, gallery, lore/background/content, metadata, relationships, related entities — not a standalone Stories publishing system)
- 4.5 — Map Data + Semantic Zoom Foundation
- 4.6 — Interactive Map Explorer
- 4.7 — Worldbuilding + Map Admin Refinement
- 4.8 — Home KRUPNI Map Integration
- 4.9 — Stabilization + Integration QA
- 4.10 — Production Close + Sprint 4 Closure

No multi-universe system is required or built here; KRUPNI remains the only active universe. Independent long-form Stories/story archive/Devlog publishing remain Sprint 8. Global Admin UI/UX redesign remains deferred to Sprint 9.

**Owner-directed Admin UI Migration — Phase 1:** **Complete.** TailAdmin was used only as a shell reference: the existing Admin now has a dark, responsive, collapsible sidebar, restrained header, real-route navigation, View Website, and the existing logout action. Existing CRUD pages, authentication, data, and public-site styling remain unchanged. Individual forms and tables are intentionally deferred to a later migration phase; this is not Sprint 5 feature work.

**Owner-directed Admin UI Migration — Phase 2 (systemic consistency pass):** **Partial — foundation and one shared pattern fixed, full page-by-page migration not done.** Root-caused and fixed the owner-reported "content not centered, asymmetric gutters" defect: `.ta-shell .adm-form` had `margin:0` (killing its inherited auto-centering) while capped narrower than its `.adm-wrap` parent, pushing every content editor (Portfolio/Sketches/3D/Games/Worldbuilding) flush left with a large dead zone on the right — confirmed as the root cause via live `getBoundingClientRect` measurements on the Worldbuilding and Portfolio edit pages before/after (equal gutters at every level post-fix, 0px overflow). Also fixed a real "NO SERIF IN ADMIN" rule violation: the sans-serif override only forced a fixed tag list, so classes on other elements (Dashboard stat numbers, Home Skills order badges) still rendered in the public site's serif font; broadened to a universal `.ta-shell, .ta-shell *` rule. Fixed the exact "raw form controls floating under the editor" defect visible in the owner's Worldbuilding screenshot: `ExtraImagesPanel`/`ExtraVideosPanel`/`ExtraLinksPanel`/`WorldbuildingRelationshipsPanel` (shared across all five content editors) now render inside the standard `AdminSection` card instead of bare `<p>` labels, grouped under one `.adm-editor-extras` wrapper on all five edit pages. **Not done in this pass:** individual redesign/reclassification of Dashboard, Home (sections 01-08 beyond Skills), Metadata, Link Labels, Appearance, About, CV, Settings, Archive, Services, and Map Editor into the archetype system described in the owner's brief; a full route-by-route 390/768/1440/1920 browser walkthrough of all ~31 Admin routes (only Worldbuilding/Portfolio editors, Dashboard, and Home were spot-checked live; `npm run qa:responsive` — public + `/admin/login` only, 20/20 — covers the routes it already knew about, not deep authenticated Admin pages); a Vercel Preview deployment (attempted via `vercel deploy`, blocked by "Not authorized" — no authenticated Vercel session in this environment). Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same 33 pre-existing warnings), `npm run build` (all 42 routes build, all 31 real Admin routes inventoried from the route tree), `npm run qa:responsive` (20/20), `git diff --check` clean. Two commits (`a1fa370`, `5bc263b`) pushed to `admin-tailadmin-migration`; `main`/Production untouched. Follow-up work (remaining pages, full visual QA, Preview deploy) is a straightforward continuation of this same pass, not a new investigation.

**Sprint 5 — Game Design + Games Admin** — Game listing and detail; mechanics, systems, GDD, and development progress; taxonomy/software; Games Admin-specific fields.

**Sprint 6 — Projects + Projects Admin** — Project data model; listing and detail shell; media/process structure; Projects Admin. A shared Content Detail Shell may only be started here if a genuine cross-content need appears — not built speculatively.

**Sprint 7 — Portfolio + Portfolio Admin** — ArtStation-referenced masonry grid; search and taxonomy filters; media-heavy detail shell; image/video/3D presentation; expansion of the Portfolio editor.

**Sprint 8 — Stories, Lore & Devlog + Admin** — Text-heavy detail shell; blog/article structure; text and media blocks; Story/Lore relations; Devlog; expansion of Creator Stats counts. Independent writing must not be forced into the Worldbuilding taxonomy — see `01_INFORMATION_ARCHITECTURE.md`, section 3.

**Sprint 9 — Explore, About, Polish & Launch** — Global `/explore`; Featured/Latest/All; shared search and filters; `/about` = About + CV + Creator Profile; detailed Skills; automatic Production Stats; final public and Admin UI/UX polish; launch QA. The single public Creator Profile (Ege Demir Ünal / TETSUNARU) lives on `/about`.

**Sprint 10 — Multi-Language Translation** — *Post-launch, optional.* Turkish, English, Japanese, Simplified Chinese. Translation Manager; taxonomy translations; Creator Profile translations; multilingual search; locale URLs and hreflang. Must not block the first completed version.

## 4. Initial Launch Scope

Functional Home Page; Worldbuilding hub centered on KRUPNI; Interactive Map Explorer MVP; Projects; Portfolio; About and Contact access; basic Stories/Writing and Devlog support; responsive design; essential accessibility; SEO foundation; stable deployment; practical content-management workflow.

## 5. Not Launch-Blocking

Genuinely optional or deferrable features — not scheduled for the initial launch, but not ruled out either, only if a real future need appears: AI search, advanced map layers, complete multi-language support, complex analytics, advanced owner/admin permission capabilities.

Public accounts, social features, and a second Worldbuilding universe are **not** deferred launch features — they are excluded entirely; see section 6, Explicitly Out of Scope.

## 6. Explicitly Out of Scope

Not future expansion, not optional post-launch, not planned architecture — outside the current product direction entirely, and introduced only if the owner explicitly changes this decision later:

- Public registration, public login, public user accounts, public profiles
- Comments, likes, reactions, emotes
- Reader bookmarks, followers, reader progress tracking tied to accounts
- Community feeds, public activity feeds, user-generated content
- In-app visitor notifications
- Public role/permission systems
- A second Worldbuilding universe or any multi-universe management system

Interface feedback (toasts, alerts, save/publish confirmations, error messages, admin feedback, loading feedback — see `06_MOTION_SYSTEM.md`) is unrelated to the above and is not affected by this exclusion. Optional Future Email Updates (section 7) is also unrelated — it is a separate, unscheduled idea distinct from in-app visitor notifications, not a way of reintroducing them.

## 7. Optional Future Email Updates

- This feature may be considered only after the website is completed and actively used.
- It should only be added if the owner feels a genuine need to help visitors remember or follow new content.
- It must not be implemented merely because newsletters are common.
- It is separate from public accounts and in-app notifications.
- Visitors would subscribe using only an email address.
- A managed email provider should be preferred over custom email-delivery infrastructure.
- Consent, unsubscribe, and privacy requirements would need to be handled.
- No subscriber table, email provider, or automated campaign system should be added now.
- This feature is not scheduled and is not assigned to Sprint 10 or any other sprint.
- RSS may also be considered as a lighter alternative.
- The decision remains open and unknown until a real need appears.

## 8. Current Status

- Sprint 3 — Home Page + Home Admin: Complete (Tasks 3.1–3.14 complete; 3.12.1B superseded). **Sprint 4 — Worldbuilding + Map: Active.** Previous sprint: Sprint 2 — Private Admin Panel and CMS Refinement (Complete). Sprint 1 — Core Systems (Complete).
- Sprint 2 final baseline commit: `bed4b9c7229518cac2357d6a1f7b7e69da6fdf49`.
- Task 1.1 — Existing Core Systems Audit: Complete.
- Task 1.2 — Final Product Scope and Documentation Alignment: Complete.
- Task 1.2.1 — Documentation Consistency Patch: Complete.
- Task 1.3 — Reduced-Motion Foundation: Complete.
- Task 1.4 — Spacing Tokens Alignment: Complete.
- Task 1.5 — Typography Tokens Alignment: Complete.
- Task 1.6 — Shared Public UI Primitives: Complete.
- Task 1.7 — Loading, Empty & Error States: Complete.
- Task 1.8 — Error Boundaries & Fallback Behaviour: Complete.
- Task 1.9 — Content Validation Foundation: Complete.
- Task 1.10 — Search, Tags, Categories & Archive Foundation: Complete.
- Task 1.11 — SEO Metadata Foundation: Complete.
- Task 1.12 — Sitemap, Robots & Indexing Rules: Complete.
- Task 1.13 — Media Optimization Foundation: Complete.
- Task 1.14 — Accessibility & Responsive Core QA: Complete.
- Task 1.15 — Performance & Vercel Usage QA: Complete.
- Task 1.16 — Sprint 1 Integration QA: Complete.
- Task 1.17 — Roadmap Status Update / Sprint Close: Complete.
- Task 2.1 — Admin Panel Audit & Scope Lock: Complete.
- Task 2.2 — Admin Shell, Navigation & Responsive Layout: Complete.
- Task 2.3 — Admin Dashboard & Content Overview: Complete.
- Task 2.4 — Shared Admin Form System: Complete.
- Task 2.5 — Permanent Browser QA Harness: Complete.
- Task 2.6 — Admin Authentication & Destructive Action Safety: Complete.
- Task 2.7 — Media Upload Safety: Complete.
- Task 2.8 — Existing CRUD Consistency: Complete.
- Task 2.9 — Controlled Metadata Foundation & Manager: Complete.
- Task 2.10 — Portfolio Metadata Selectors & Legacy Migration: Complete.
- Task 2.11 — Admin Integration QA: Complete.
- Task 2.12 — Production Sync & Sprint 2 Close: Complete.
- Task 3.1 — Existing Home System Audit + Scope Lock: Complete.
- Task 3.2 — Home Data Architecture: Complete.
- Task 3.3 — Home Admin Architecture: Complete.
- Task 3.4 — Identity Hero: Complete.
- Task 3.5 — Selected Work Coverflow: Complete.
- Task 3.6 — Capabilities + Home Skills: Complete.
- Task 3.7 — Automatic Production Stats: Complete.
- Task 3.8 — KRUPNI Home Map Preview: **Complete.**
- Task 3.9 — Worldbuilding Highlights: **Complete.**
- Task 3.10 — Latest Dispatches: **Complete.**
- Task 3.11 — Contact + Social: **Complete.**
- Task 3.12 — Responsive + Motion + Accessibility + Performance: **Complete.** — 3.12.1 Public Home Refinement: **Complete** for current Sprint progression; remaining non-blocking final aesthetic polish deferred; Home KRUPNI map integration remains deferred to Sprint 4. 3.12.1B Home Visual Composition Correction: **Superseded / visually insufficient.** 3.12.1C Home Visual Redesign & Acceptance: **Complete.** 3.12.1D Home Centered Composition & Reference Alignment Correction: **Complete.** 3.12.1E Contact Composition Redesign: **Complete.** 3.12.1F Contact Screenshot-Matching Pass (Final): **Complete.** 3.12.1G Contact Sliding-Image Orientation Fix: **Complete.** 3.12.1H Contact Banner Proportion Correction: **Complete.** 3.12.2 Shared Admin Visual Refinement: **Complete.** 3.12.3 Responsive + Motion + Accessibility + Performance Stabilization: **Complete.**
- Task 3.13 — Integration + Browser QA: **Complete.**
- Task 3.14 — Production Deploy + Sprint Close: **Complete.**
- Task 4.1 — Worldbuilding + Map Audit & Scope Lock: **Complete.** Audit-only; no runtime code changed. Verified findings:
  - **PRESERVE:** Public `/worldbuilding` (`WorldbuildingBrowser`/`WorldbuildingGrid`/`WorldbuildingControls`) with search, category pills, and a default/grid view toggle; the shared `GalleryModal` detail experience with images/videos/links/rich `content`; the existing `worldbuilding_entries` CRUD, image/video/link sub-resource actions, and admin form/preview (`WorldbuildingForm`); `world_maps` + `map_locations` schema with real parent/submap hierarchy (`parentMapId`/`targetMapId`), pin→lore linking (`entryId`), and `x`/`y` percent coordinates; the public static-preview-to-fullscreen-explorer pattern (`WorldbuildingAtlas` → `MapZoomPanel`) with wheel-zoom, pointer-based drag-pan (touch-capable), breadcrumb submap navigation, and pin-click routing to either a submap, a lore entry, or an inline info/image panel; the admin `MapEditor` with pointer-event pin placement/drag (touch-capable) and a full pin-detail edit panel (`MapPinEditPanel`); `requireAdminSession()` on every worldbuilding/map mutation.
  - **EXTEND:** The existing `wb-square-grid` grid-view mode (image-first square tiles) is the closest foundation for the Task 4.3 ArtStation-style discovery grid but is a secondary toggle, not the primary browsing mode, and cards elsewhere are text-led, not media-first; `GalleryModal` is a reusable foundation for the Task 4.4 Content Detail Shell but has no relationship/related-entity section; `world_maps.parentMapId` hierarchy is a real foundation for Task 4.5's zoom/layer work; `mapLocations.pinType`/`iconType` (free-text columns) are a foundation for a Task 4.5 layer/category model.
  - **MISSING:** `CategoryType` (`Characters, Cities, Systems, Factions, Items, History` — `types/worldbuilding.ts`) does not match Sprint 4's required taxonomy (Character, Location, Corporation, Technology, Lore) — Task 4.2 must reconcile this. No type-specific admin fields exist for any category (one generic form for all). No Worldbuilding entity-relationship model exists (no entry↔entry table/field). `mapLocations` has no `priority`, `minZoom`, `maxZoom`, or `layer`/category column — every pin renders unconditionally regardless of zoom level; there is no semantic-zoom or layer/legend system today. The public map explorer has no pinch-to-zoom (only wheel/+/− buttons); the admin `MapEditor`'s canvas pan/zoom (`useMapPanZoom`) is mouse-event-only (`onMouseDown`/`onMouseMove`/`onMouseUp`), not pointer-event-based, so it lacks touch pan/zoom support even though pin placement/drag on the same canvas already uses pointer events.
  - **DEFER:** Independent Stories/story archive/Devlog publishing (Sprint 8); global Admin UI/UX redesign (Sprint 9); multi-universe architecture (excluded entirely, see Section 6).
  - **BLOCKER:** None found — nothing prevents starting Task 4.2.
  - **Schema implications (verified, not yet applied):** Task 4.2 will need an additive `type`/taxonomy column (or a controlled-metadata-style category refactor) on `worldbuilding_entries` to replace/reconcile the current `cat` free-text field with the Character/Location/Corporation/Technology/Lore taxonomy, plus a genuine entity-relationship join table (none exists today). Task 4.5 will need additive columns on `map_locations` for semantic zoom (`priority`, `minZoom`/`maxZoom`) and, only if a real layer/legend need is confirmed while building it, a `layer`/category column — `pinType`/`iconType` already exist as free-text and are reusable rather than replaced. No migration was created in this task, per instruction.
- Task 4.2 — Worldbuilding Data + Taxonomy + Relations: **Complete.** Added canonical `character`, `location`, `corporation`, `technology`, and `lore` types through a nullable additive field; legacy taxonomy is preserved and ambiguous values remain unassigned. Added an indexed, cascading entity-relationship foundation with self-relation and exact-duplicate protection. Task 4.3 is next.
- Task 4.3 — Public Worldbuilding Visual Discovery Grid + Filters: **Complete.** Worldbuilding discovery is now media-first / ArtStation-inspired: the old dual-mode (`.wb-grid` text-led default + `.wb-square-grid` secondary toggle) was replaced with a single dense `.wb-disc-grid` — minimal 4px gaps, deterministic (id-based, not random) varied tile heights standing in for unstored per-image aspect ratios, mostly-image cards that reveal a type badge + title on hover/focus (`:focus-visible` parity with `:hover`), no invented stats/likes/views. The primary filter is now the canonical `entityType` (All/Character/Location/Corporation/Technology/Lore) from Task 4.2, replacing the legacy `cat` pills as the public control; legacy `cat` remains as the hover-badge fallback for records with no canonical type, which stay visible under All (never guessed into a type) with a clean empty state on a canonical filter with zero matches. `GalleryModal` open/close/keyboard behavior is unchanged (Task 4.4 owns the detail shell). Verified: `tsc --noEmit`/lint (0 errors, same 28 pre-existing warnings)/build/`qa:responsive` (20/20, 0 overflow/console errors) all passed; a real click-to-open/Escape-close/Enter-key-open/filter/empty-state/reset-to-All interaction pass ran clean against a production build via a dependency-free CDP script (dev-mode Turbopack hydration timing and a devicePixelRatio/viewport coordinate mismatch in the interactive browser tool had produced false negatives earlier in the same investigation — traced to the test harness, not the app, and confirmed via a `git stash` re-test against the untouched Task 4.2 baseline). Task 4.4 is next.
  - **Visual correction (same-task follow-up):** the initial pass still diverged too far from the owner's direct ArtStation Explore "All Channels" screenshot reference. Corrected: `.wb-disc-grid` switched from `auto-fill` to `auto-fit` (the root cause of the large dead space on the right — `auto-fill` was reserving empty 1fr tracks past the item count) so the grid now uses the full available width; the deterministic tall/short/wide masonry spans were replaced with a uniform `aspect-ratio:4/5` cell (object-fit:cover crop) per the owner's explicit instruction to prefer consistent discovery-cell sizing over faked masonry; `.wb-ctrl` was flattened from a boxed/bordered panel to a slim flush control strip (compact search input, smaller pills, no background card). Same functionality, filters, legacy-data handling, and `GalleryModal` behavior — re-verified after the correction.
  - **Square-gallery correction (same-task follow-up):** revised the uniform discovery media cells from `4:5` to strict `1:1` square thumbnails, with `object-fit:cover` preserving clean, undistorted crops. The compact footer remains below (never within) the square and is fixed to the metadata line and title only, eliminating the Lore-only teaser so cards retain a consistent media-first gallery rhythm. Filters, legacy-data handling, and `GalleryModal` behavior remain unchanged.
- Task 4.4 — Shared Worldbuilding Content Detail Shell: **Complete.** The existing `GalleryModal` (opened via the canonical `/worldbuilding?item=<id>` route already used site-wide) was extended in place, per Task 4.1's finding that it is the reusable foundation for this shell but lacked a relationship section — no new detail route or competing detail system was introduced. Added: an optional `related`/`onRelatedSelect` extension to `GalleryModal` (Portfolio/Games/Sketch/Model3D consumers unaffected, since they never populate it) rendering a "Related Worldbuilding Entries" section — clickable, keyboard-accessible (Enter/Space, focus-visible), thumbnail + type + title cards — sourced from the Task 4.2 relationship table (bulk-fetched once, resolved client-side against the already-loaded entry list, no N+1 queries); the section is omitted entirely when an entry has no relationships (no empty decorative sections). The sidebar's top type badge and the new related cards both resolve their label through a shared `resolveEntityTypeLabel` helper (canonical `entityType` when classified, legacy `cat` otherwise), keeping the Task 4.3 grid and the Task 4.4 detail shell in agreement; `WorldbuildingGrid` was refactored to reuse the same helper (no behavior change). `GalleryModal`'s existing media-dominant layout (large image/gallery side + secondary info sidebar) and its `gm-reading` long-form mode (large title, small metadata, TOC, restrained column, section headings, sans-serif throughout) already matched the owner's TARGET A / TARGET B-C references and needed no structural rework — untyped legacy entries continue to render through the same shell with no invented fields. Verified: `tsc --noEmit`/lint (0 errors, same pre-existing warning set)/build/`qa:responsive` (20/20, 0 overflow/console errors) all passed; a real click-to-open, related-card click-to-navigate, and empty-related-section (no relationships exist yet in the live data — all current entries are legacy/unclassified) interaction pass ran in-browser via CDP, including a temporary in-memory-only related-entries override used solely to visually confirm the new section and its navigation before being reverted (no data was written to the database). Task 4.5 is next.
- Task 4.4B — Worldbuilding Detail Shell Visual Reconstruction: **Complete.** Task 4.4's self-assessment that the layout "already matched" the owner's references was visually incorrect — screenshot evidence showed the shipped modal still reading as a medium centered card (max-width 1100px, 380px rail), not the required viewport-scale artwork viewer / full-bleed reading page. Reconstructed both compositions in place on the same `GalleryModal` shell (still the single detail system, same `/worldbuilding?item=<id>` route, same open/close/keyboard/related/relationship behavior) via a new opt-in `variant="worldbuilding"` prop, defaulting to `"default"` — every other consumer (Portfolio/Games/Sketches/3D) omits the prop and is pixel-identical to before, confirmed by including `/portfolio` in this task's `qa:responsive` pass. **Media/artwork mode** (`.gm-panel--wb`): panel grows from a 1100px-capped card to `min(96vw,1900px)` at 94vh (measured live: 1900×960 inside a 2048×1022 viewport, ~79%/21% media/rail split, ~74px outer margins — closely matching TARGET A's near-full-bleed geometry); Prev/Next move from a small bottom-center pair to circular controls pinned to the left/right edges of the media area, matching TARGET A's arrow placement; the existing Year/Date/desc/tags/related/links rail is unchanged in content, only in size (already the right shape per Task 4.1, just too narrow a stage). **Reading/lore mode** (`.gm-panel--wb-reading`, added only once an entry has real `content`): the persistent right rail is no longer rendered at all — title/category/close, Year/Date, and TOC move inline as a `.gm-reading-header` atop a single centered ~720px prose column, tags/related/links move to a `.gm-reading-footer` after the article, article images/covers bleed to `calc(100% + 240px)` past the prose column (TARGET C), and the column's `--T`/`--B` font tokens are locally overridden to the design system's existing sans-serif (`--S`/Archivo) via the same scoped-custom-property technique `.home-page` already uses — closing the "serif in reading mode" gap the task explicitly called out. **Responsive:** the pre-existing ≤820px `.gm-panel{grid-template-columns:1fr}` stacking rule now also governs both new variants (same class, later cascade position, no new media query needed for the base stack); added explicit `.gm-panel--wb-reading .gm-img-side{max-height:none}` so that rule's `max-height:300px` (meant for the default image-on-top-of-info stack) can't clip the reading column to a tiny scroll box. **Visual verification (mandatory, not skipped):** real in-browser screenshots plus `getBoundingClientRect` measurements confirmed viewer-mode geometry and edge-nav placement at desktop; reading mode was verified against a temporary, local-only `content` fixture on one entry (no current data has rich `content` yet, per Task 4.4's own finding) — reverted via `git diff` before commit, confirmed byte-identical to pre-fixture except the one intended `variant` line; a standalone, dependency-free CDP script (same technique as `scripts/responsive-qa.mjs`, deleted after use) confirmed zero horizontal overflow and correct single-column/stacked-info layouts for both modes at 390×844. Verified: `tsc --noEmit` (0 errors), `npm run lint` (0 errors, same 29 pre-existing warnings), `npm run build`, `npm run qa:responsive` (20/20 including `/portfolio`), and `git diff --check` all passed. Task 4.6 remains next (not started).
- Task 4.5 — Map Data + Semantic Zoom Foundation: **Complete.** Data/foundation only — no new Map Explorer UI, per Task 4.1's finding that `world_maps.parentMapId` and `mapLocations.pinType`/`iconType` are extension points, not replacements. Added three additive `map_locations` columns: `priority` (integer, default `0`, bounded `0-100`, tie-broken by `id` for deterministic ordering — not a visibility filter by itself), `minZoom`/`maxZoom` (integer, default `1`/`5`, bounded `1-5` each with `minZoom <= maxZoom` enforced by both a DB `CHECK` and shared server-side validation). The 1-5 range is an abstract, viewer-independent zoom scale (documented in `lib/map-zoom.ts`) that Task 4.6 normalizes each viewer's own pixel zoom into (public `MapZoomPanel` spans 1-4, admin `MapEditor` 1-3) rather than a literal zoom-percentage column tied to one viewer. **Layer decision:** no dedicated `layer` column was added — `mapLocations.iconType` (city/character/landmark/hazard/default) already is a small, controlled, legend-able category, so a second parallel field would have duplicated it; `lib/map-zoom.ts`'s `groupByIconType()` is the grouping helper Task 4.6's layer/legend UI can use directly. New `lib/map-zoom.ts`: `isMarkerVisibleAtZoom`/`visibleMarkersAtZoom` (inclusive-boundary zoom-visibility check), `sortByPriority` (deterministic priority-desc/id-asc ordering), `getChildMaps`/`getMapBreadcrumb` (pure re-expressions of the parent/submap walk `MapZoomPanel` already performs inline, exposed for reuse without touching that component), `groupByIconType`, plus the shared `validatePriority`/`validateZoomRange` validators (built on a new generic `boundedInt()` in `lib/validation.ts`) — centralizing every bound so Task 4.6 and future Admin UI reuse the same rules the DB enforces. New `updateMapLocationZoom` server action (`lib/actions/map.ts`) — guarded by `requireAdminSession()`, validated through these helpers, silently rejecting invalid input like the other typed sub-resource actions (`createMapLocation`, `updateMapLocationPosition`) since no error-display UI calls it yet; no existing action's signature or behavior changed, so `MapEditor`/`MapPinEditPanel` needed no edits — wiring this into the Admin UI is Task 4.7's scope. Purely additive migration `drizzle/0022_shocking_madame_web.sql` (three `ALTER TABLE ... ADD COLUMN`, one index, four `CHECK` constraints — no drops, no rewrites, no data changes), applied via `drizzle-kit migrate` after explicit owner approval (the build's own prerender step failed against the live schema before migration, confirming it was genuinely unapplied, and succeeded immediately after). A pre-existing mismatch was found and corrected during this task: `drizzle-kit migrate`'s default env loading only reads `.env`, while `next build`/the app's own runtime prioritize `.env.local` — the migration was applied to both to keep them consistent, and the correct one (`.env.local`, confirmed by re-running `npm run build` successfully afterward) was verified as the one the app actually queries; flagged here as a real gap in the migration script's env loading, not fixed further since changing `drizzle.config.ts`'s env behavior is outside this task's additive-migration-only scope. Verified with a temporary, read-only script against the live database (deleted after use): both existing `map_locations` rows kept their pre-migration `x`/`y`/`pinType`/etc. and now show priority `0`/minZoom `1`/maxZoom `5` (0 rows deviating from the default), both are visible at zoom `1` and zoom `5` (the full range, preserving current always-visible behavior), `sortByPriority` produced a stable order, and `getChildMaps`/`getMapBreadcrumb` returned correct results against the real `world_maps` hierarchy; a second temporary script confirmed `validatePriority`/`validateZoomRange` accept every in-range value and reject out-of-range, `NaN`, non-integer, and `minZoom > maxZoom` inputs. `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same 29 pre-existing warnings, confirmed unrelated to this task via `git stash`), `npm run build` (all routes including `/admin/worldbuilding/map` build successfully post-migration), and `git diff --check` all passed. Task 4.6 is next.
- Task 4.6 — Interactive Map Explorer: **Complete.** Turned the existing pan/zoom foundation (`MapZoomPanel`/`WorldbuildingAtlas`/`useDragZoom`) into the real public explorer — no rebuild, no new map dependency. **Semantic zoom:** `MapZoomPanel` normalizes its own pixel zoom (1-4) into Task 4.5's abstract 1-5 scale (`toSemanticZoom`) and now renders through `visibleMarkersAtZoom`/`sortByPriority` (`lib/map-zoom.ts`) instead of an unfiltered list — the two real migrated pins (`priority=0`, `minZoom=1`, `maxZoom=5`) stay visible across the full zoom range, matching the required always-visible contract; `WorldbuildingAtlas`'s static preview reads as the fully-zoomed-out (semantic zoom 1) view through the same helper, so both viewers agree. **Pan + zoom:** `useDragZoom` (shared with `ImageZoomOverlay`, unaffected — it never uses the pointer-event path touched here) gained real two-finger pinch-to-zoom, tracked via a pointer map that takes over from single-finger drag as soon as a second pointer lands and hands back cleanly on lift; existing mouse-drag/wheel-zoom/+/−/Reset behavior is unchanged. `.mzp-viewport` now sets `touch-action:none` so native browser pinch/scroll gestures can't fight the panel's own handling. **Hierarchy:** added a compact submap navigation row (`getChildMaps`) alongside the existing parent breadcrumb, so `World Map → Krup` (the real `parentMapId` hierarchy) is reachable directly, not only via clicking a submap pin — no invented map/continent data. **Markers:** pins now carry a real accessible name (`aria-label`, "Open X submap" / "View X") instead of relying on visually-hidden label text, plus a `:focus-visible` outline (previously missing on map pins, breadcrumb links, and zoom-toolbar buttons — added to all three since they're the same interaction surface this task owns); pin→lore routing to the canonical `GalleryModal` (`variant="worldbuilding"`) is unchanged. **Legend:** a compact `.mzp-legend` renders only when a map's pins mix more than one `iconType` (Task 4.5's decision — no dedicated layer column); with the current real data (World Map mixes `character`+`city`) it renders two entries, and would stay hidden entirely on a single-category map, per the task's explicit "don't show a useless control" instruction. **Reduced motion:** the map-transition (framer-motion opacity/scale on submap navigation) now checks `usePrefersReducedMotion()` (the same hook `HeroCarousel`/`Reveal`/`SelectedWorkCoverflow` already use) and renders as an instant state change instead of an animated one when reduced motion is requested — this transition predates Task 4.6 but sat in code this task was already modifying for hierarchy nav, and was a real gap against the "reduced-motion safe transitions" requirement. Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same 29 pre-existing warnings, no new ones), `npm run build` (all routes build), `npm run qa:responsive` against a real local dev server (16/16 at 390×844/768×1024/1440×900/1920×1080 on `/`, `/worldbuilding`, `/about`, `/admin/login` — 0 overflow, 0 console errors), `git diff --check` clean, and a standalone pure-logic check confirming `toSemanticZoom`'s boundary mapping (explorer zoom 1→semantic 1, 4→semantic 5) and the pinch distance-ratio-to-zoom math independent of any browser. **Known limitation, not a regression:** live click/keyboard/pinch interaction QA in-browser could not be completed this session — real clicks via the interactive browser tool, raw DOM `dispatchEvent`, and a standalone CDP `Input.dispatchMouseEvent`/`dispatchTouchEvent` script all failed to deliver any event to the page, reproduced on a pre-existing, unmodified control (a Task 4.3 filter pill) with the same non-response, confirming it as a browser-automation environment limitation for this session rather than anything introduced here; code review, the pure-logic check, and the passing automated checks above stand in for it, per the owner's explicit direction to proceed on that basis. The owner is expected to manually verify pan/zoom/pinch/marker-click/hierarchy-nav on the deployed production URL. Task 4.7 is next.
- Task 4.4C — Separate Worldbuilding Artwork Detail and Lore Reader Visual Systems (visual architecture correction): **Complete.** Corrects Task 4.4B's decision: reusing one `GalleryModal` shell with CSS modifiers (`variant="worldbuilding"`, `.gm-panel--wb`/`.gm-panel--wb-reading`) was visually insufficient — the owner confirmed ArtStation itself uses two genuinely different presentation systems for artwork posts vs. blog/article posts, and the shipped shell still read as one viewer with text swapped in, not two distinct experiences. Replaced with two standalone, independently-styled public components, neither of which imports `GalleryModal`: `components/worldbuilding/WorldbuildingArtworkDetail.tsx` (`.wbd-*` classes — large edge-to-edge media stage + right info rail: type badge, close, title, Year/Date, description, tags, related entries, links, circular prev/next controls pinned to the media area's edges) for Character/Location/Corporation/Technology and any untyped legacy entry; and `components/worldbuilding/WorldbuildingLoreReader.tsx` (`.wbr-*` classes — full dark reading surface, sticky breadcrumb/close bar, centered ~720px article column, large title, small Year/Date meta line, a TOC pill row, section headings, paragraphs, inline images that bleed to `calc(100% + 240px)` past the column, tags/related/links footer, fixed edge chevrons for prev/next) for canonical `entityType === "lore"` entries only. `GalleryModal.tsx` was reverted to its pre-4.4B baseline (no `variant` prop, no `related`/`onRelatedSelect`, no wb CSS) since Portfolio/Games/Sketches/3D never used those additions — confirmed unaffected via `qa:responsive` including `/portfolio`. `WorldbuildingBrowser.tsx` now renders `WorldbuildingLoreReader` when the currently open entry's canonical `entityType === "lore"`, else `WorldbuildingArtworkDetail` — legacy/untyped entries are never guessed into Lore, matching Task 4.2's rule. Shared, low-level primitives only (not shared visual shell): the `GalleryItem` type, `useModalFocus`, `ImageZoomOverlay`, `parseContent`/`extractHeadings`, `downloadUrl`, `toEmbedUrl`, and a new small `lib/content-sequence.ts` (`buildMediaSequence`) factoring out the image/video merge-sort the two new components both need. `app/globals.css`: removed the `.gm-panel--wb`/`.gm-panel--wb-reading`/`.gm-overlay--wb`/`.gm-reading-header`/`.gm-reading-footer` block entirely and added dedicated `.wbd-*`/`.wbr-*` rule sets (including their own ≤820px responsive rules — single-column stage for Artwork Detail, hidden edge-chevrons and reduced padding for Lore Reader). **Visual verification (mandatory, performed):** real in-browser screenshots at 1920×1080 confirmed the Artwork Detail viewer against the ArtStation artwork-viewer reference (near-full-bleed geometry, thin accent divider, circular edge arrows — no boxed/margined "card" look, unlike the pre-correction screenshot the owner supplied) using a real media-heavy entry (`?item=2`). No real Lore entry exists yet in the live data (confirmed via a temporary, read-only script, deleted after use); verified the Lore Reader against both ArtStation Breakdown references using a temporary, LOCAL-only in-memory `content`/`entityType` fixture on one entry (`app/(site)/worldbuilding/page.tsx`, one existing entry only) — breadcrumb bar, large title, small metadata line, TOC pills, wide cover, red section headings, prose rhythm, a wider-than-column inline image, and the tags/desc footer all matched; reverted via `git checkout` before commit, confirmed byte-identical to pre-fixture (`git diff --stat` showed zero changes to that file). A standalone, dependency-free CDP script (same technique as `scripts/responsive-qa.mjs`, deleted after use) confirmed zero horizontal overflow for both the Artwork Detail and Lore Reader open-modal states at both 390×844 and 768×1024. Verified: `tsc --noEmit` (0 errors), `npm run lint` (0 errors, 35 warnings — the same pre-existing `<img>`/hook-dependency pattern extended to the two new files, no new categories), `npm run build`, `npm run qa:responsive` run twice — once against local dev (20/20) and once against a local production build/`npm run start` (20/20, one tolerated automatic Chrome relaunch per Task 2.5's documented recovery behavior) — and `git diff --check` all passed; only `app/globals.css`, `components/GalleryModal.tsx`, `components/WorldbuildingBrowser.tsx`, the two new `components/worldbuilding/*.tsx` files, and new `lib/content-sequence.ts` changed. Task 4.4/4.4B/4.5/4.6 remain Complete — this task corrects Task 4.4B's visual-architecture decision, not its data/foundation work. Task 4.7 remains next (not started).
- Task 4.7 — Worldbuilding + Map Admin Refinement: **Complete.** Owner Admin controls for the Sprint 4 systems, no global Admin redesign (deferred to Sprint 9). **Worldbuilding entity type:** `WorldbuildingForm` gained a controlled `entityType` Select (Character/Location/Corporation/Technology/Lore, from Task 4.2's `WORLDBUILDING_ENTITY_TYPES`) alongside the untouched legacy `cat` field — the mutation boundary (`readOptionalEntityType` in `lib/actions/worldbuilding.ts`) already supported this exact set-or-clear contract from Task 4.2, so only the UI was missing; selecting Lore now makes that entry render through the real `WorldbuildingLoreReader` on `/worldbuilding` (Character/Location/Corporation/Technology and untyped legacy entries continue through `WorldbuildingArtworkDetail`), confirmed by the existing `WorldbuildingBrowser` routing from Task 4.4C — no new public code needed. **Relationships:** new `WorldbuildingRelationshipsPanel` on the entry edit page lists relationships from either direction (via the existing `getRelatedWorldbuildingEntitySummaries` helper) with add (target entry + free-text relationship label, matching the Task 4.2 schema's own unconstrained `relationshipType` column) and remove, reusing `createWorldbuildingRelationship`/`deleteWorldbuildingRelationship` — both already existed in `lib/actions/worldbuilding.ts` from Task 4.2/4.6 with self-relation and exact-duplicate protection built in; `deleteWorldbuildingRelationship`'s return type was normalized to `void` (matching every other sub-resource delete action) to satisfy the form-action type. **Map semantic zoom:** `MapPinEditPanel` gained Priority/Min Zoom/Max Zoom number inputs (bounds and help text sourced from `lib/map-zoom.ts`'s existing constants, client-side min≤max check before calling the pre-existing `updateMapLocationZoom` action from Task 4.5) plus Priority/Zoom Range columns in `MapEditor`'s pin table. **Touch/pointer:** `useMapPanZoom`'s viewport pan handlers were converted from mouse-only (`onMouseDown`/`onMouseMove`/`onMouseUp`) to Pointer Events with pointer capture, matching the pin drag handlers on the same canvas that already used Pointer Events (Task 4.1's finding); `.map-viewport` gained `touch-action:none` so native touch-scroll can't fight the pan; pin drag continues to stop propagation before it reaches the viewport, so panning a gesture that starts on a pin still only drags that pin. **Controlled Link Label selector:** new `link_label_options` table (id/label/slug/sortOrder/isActive — no `type` grouping, since there's only one label list; smallest schema Task 4.7's brief asked for) seeded with ArtStation/YouTube/Steam/itch.io/GitHub/Website/Trailer/PDF/Download; a new `/admin/link-labels` manager (mirrors the Task 2.9 Metadata Manager pattern at reduced scope: no icon upload — `slug` is deliberately the only future logo-lookup key, per this task's own "no logo UI yet" instruction) lets the owner add/edit/reorder/deactivate/delete options (deletion is always safe — `*_links.label` stays plain text with no FK). `ExtraLinksPanel`/`AddLinkForm` (shared by Portfolio/Sketches/3D/Games/Worldbuilding — confirmed the single shared editor before touching it) now render the Label field as a `<select>` sourced from `getActiveLinkLabelOptions()`; a link's already-saved label that isn't in the controlled list (confirmed by inspecting live data first: one legacy `"Yarış"` Portfolio link, one differently-cased `"Artstation"` Game link) is injected as its own "(legacy)" option on that row only, so no existing link data was rewritten or dropped. **Migration:** `drizzle/0023_link_label_options.sql` — one additive `CREATE TABLE`, one unique index on `slug`, nine `INSERT ... ON CONFLICT DO NOTHING` seed rows; no drops, no rewrites, no existing table touched. Applied via `drizzle-kit migrate` after explicit owner approval, to **both** `.env`'s and `.env.local`'s databases (the same drift Task 4.5 flagged — `drizzle-kit migrate` only reads `.env` by default while the app runtime uses `.env.local` — worked around the same way, by pointing `DATABASE_URL` at each in turn) and verified with a temporary, read-only script (deleted after use) confirming all 9 seed rows exist in both. Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same 35 pre-existing warnings — same categories as Task 4.4C, no new ones, confirmed by diffing the warning file list), `npm run build` (all routes build, including the two new/changed dynamic Admin routes `/admin/link-labels` and `/admin/worldbuilding/[id]/edit` against the now-migrated live schema), `npm run qa:responsive` against a real dev server (19/20 passed, 1 skipped on a tolerated automatic Chrome relaunch per Task 2.5's documented recovery behavior — same flake pattern as Task 4.4C, not a regression), and `git diff --check` clean. Authenticated in-browser Admin interaction (actually logging in and exercising the new controls) was intentionally not performed by automation — entering the Admin password through browser automation is outside this session's permitted actions — so the owner should manually verify the new controls (entity type picker, relationships panel, map zoom fields, touch pan on a tablet/phone, Link Label selector) against Production after deploy, per this task's own QA section. Task 4.8 is next.
- Task 4.8 — Home KRUPNI Map Integration: **Complete.** Home now presents a larger atlas-first preview powered by the Sprint 4 map system: the oversized KRUPNI heading is removed, all valid `world_maps`/`map_locations` data and semantic-zoom rules are retained, real submap/detail marker destinations remain actionable, and Home opens the existing `MapZoomPanel` large explorer. **4.8B correction:** the Home map surface now opens the viewer (separate Open Atlas button removed), the cover-framed preview removes black side gaps, the embedded Worldbuilding map has tighter artwork framing, and the fullscreen minimum zoom/reset deterministically restores its initial fit.
- Task 4.9 — Stabilization + Integration QA: **Complete.** No Sprint 4 runtime regression/blocker was found; no application code changed. Targeted production-build responsive QA passed at 390×844, 768×1024, 1440×900, and 1920×1080 with zero overflow, console errors, or uncaught exceptions on `/`, `/worldbuilding`, `/about`, and `/admin/login`. The browser automation environment did not dispatch focused React interaction events, so owner confirmation remains for live map pan/zoom/pinch/reset, marker/submap navigation, real Lore rendering (no production Lore record), and authenticated Admin controls. Production deployment is Ready and its required database configuration is present. Task 4.10 is next.
- Task 4.10 — Production Close + Sprint 4 Closure: **Complete.** Production now serves the latest Sprint 4 runtime deployment; the public smoke pass confirmed `/`, `/worldbuilding`, `/about`, and `/admin/login`, including the Worldbuilding discovery surface, real artwork detail, embedded/Home/large Atlas availability, and current map media/data. No Sprint 4 blocker was found and no runtime code changed. **Non-blocking owner verification remaining:** test real map touch/pan/pinch/marker/reset interactions; classify or create a real Lore entry and confirm the Lore Reader; manually exercise authenticated Sprint 4 Admin controls. These do not prevent closure. **Next:** Sprint 5 — Game Design + Games Admin.
- **Sprint 4 Closure Hotfix — Map/Pin System Repair (post-close):** Consolidated Home and Worldbuilding onto one canonical map surface: new shared `components/WorldMapArtwork.tsx` (single intrinsic-image-fit + marker layer, used by the Home preview, the Worldbuilding static preview, and the fullscreen `MapZoomPanel`) and a new `resolvePinTarget()` (`lib/map-zoom.ts`) — the one marker-action rule (submap / lore entry / info-only detail / no valid target, based only on stored fields, never a name-based special case) every surface now calls instead of each re-deriving its own routing. Fixed two real, verified cross-surface inconsistencies this closed: the Worldbuilding static preview always opened its fullscreen panel at the root map regardless of which pin was clicked (a submap pin now opens that exact submap, matching Home and the fullscreen viewer); Home's info-only pins (no submap/entry target) opened a generic, unfocused map view instead of that pin's own detail panel (`MapZoomPanel` gained an `initialPinId` prop so any surface can hand off directly to the pin's canonical detail, shown identically everywhere). Restored Home's passive-by-default interaction (lost in the prior hotfix commit): hover/wheel do nothing until an explicit click on a non-marker area activates in-place pan/zoom (shared `useDragZoom`, `interactionEnabled` gated), Escape deactivates and returns focus, and the always-present +/-/Reset controls work regardless of activation state — verified via real dispatched pointer/wheel/keyboard events (page scroll passes through while passive, wheel zooms and blocks page scroll once active, Reset/zoom-in/out work while passive without activating). Fixed two Admin `MapEditor` stale-state bugs: switching maps left the previous map's selected/editing pin and in-progress "placing" state attached to the newly-shown map (now cleared on switch); `useMapPanZoom`'s zoom/scroll position carried over from the previously-viewed map instead of resetting to the fitted view (now reset alongside the existing aspect-ratio reset, same effect/comment). Verified live on production with real data (not fabricated): the real `BASE` pin currently has no configured submap/entry target (image-only) — Home, the Worldbuilding static preview, and the fullscreen Atlas all now show its own detail panel identically, confirmed via dispatched DOM events against `https://portfolio-website-eight-xi-79.vercel.app`; the fullscreen zoom-in → pan-to-corner → zoom-out → Reset sequence was exercised locally and returns to the exact original fitted transform (`translate(0px,0px) scale(1)`), matching the frame rect byte-for-byte. Authenticated Admin CRUD interaction was code-reviewed only (session-cookie injection for local QA was blocked by this environment's own permission classifier) — owner should manually re-verify Admin Map Pins map-switching/pin-editing behavior against production. `npx tsc --noEmit`, `npm run lint` (0 errors, no new warnings), `npm run build`, and `npm run qa:responsive` (16/16 at 390×844/768×1024/1440×900/1920×1080) all passed; only the map/pin files above changed. Deployed to production (`vercel deploy --prod`) and confirmed live.
  - **Same-day owner follow-up:** the passive/in-place-pan-zoom Home behavior above was itself the reported problem — the owner wanted Home's map to behave exactly like Worldbuilding's (click opens the same fullscreen `MapZoomPanel`, not an in-place zoom), and reported the map rendering far too small. Root cause of the sizing bug: `.home-map-preview` is wrapped in a bare `<Reveal>` at the page level (`app/(site)/page.tsx`), and `.home-page`'s `display:flex;align-items:center` column layout shrinks any flex child with no explicit width to its content size instead of filling the row — every other full-bleed Home section already guards against this with its own `width:100%` (`.home-hero-band`, `.home-practice`, etc.), just missed here since this section's `<Reveal>` wraps the whole section instead of an inner block. Fixed with a new `.home-map-reveal{width:100%}` (via `<Reveal className="home-map-reveal">`). `components/HomeInteractiveMap.tsx` was rewritten to the same template/logic as `WorldbuildingAtlas.tsx` (static preview, click opens `MapZoomPanel` via the same `resolvePinTarget`/`initialPinId` wiring, no separate in-place pan/zoom/controls), and `.hmp-interactive` lost its dark boxed background/border (now `width:min(1720px,100%)`, transparent `.wa-viewport`) so the map itself is the visual, matching the owner's "no background, just the map, as large as What I Do" request — confirmed live at 1720px wide (wider than `.hp-capabilities`'s 1512px). `npx tsc --noEmit`, `npm run lint` (0 new warnings), `npm run build`, and `npm run qa:responsive` (16/16) all passed; verified live on production (click opens the fullscreen panel, Escape closes, no console errors).
- **Sprint 4 Closure Hotfix — Unified Media Lightbox (Faz 1 of 2, post-close):** Replaced the three separate "image + detail panel" systems (`GalleryModal` — Portfolio/Sketches/3D/Games; `WorldbuildingArtworkDetail` — Worldbuilding grid/entry pins; `MapZoomPanel`'s inline `.pin-panel` — map info-only pins, incl. Home) with one canonical `components/shared/MediaLightbox.tsx`, built on `WorldbuildingArtworkDetail`'s edge-pinned-arrow layout (the owner's preferred visual system). New `richReading` prop (default off) preserves Portfolio/Sketches/3D/Games' interleaved content/TOC/Features behavior without changing Worldbuilding's simpler flat body. New single-vs-multi-media behavior: exactly one image sits flush against every edge (`object-fit:contain`, zero padding); two or more use symmetric padding with the first ("hero") media element filling the scroll area's visible viewport so later media stays below the fold until scrolled, fixing the previous asymmetric-bottom-padding bug (a leftover bottom-docked Prev/Next bar) and the map pin popup's flex-centering asymmetry (now the same fixed-height Grid layout as everywhere else). Added an optional per-image `caption` (nullable, shown under the image in the lightbox) to all five gallery-image tables (`worldbuilding_images`, `portfolio_images`, `sketch_images`, `model_3d_images`, `game_images` — additive migration `drizzle/0024_complete_firelord.sql`) with an admin `ExtraImagesPanel` input. Fixed a real, verified public bug traced to a single root cause: `.wb-disc-grid`'s `auto-fit`/`minmax(150px,1fr)` columns stretched to fill the row whenever a filter (e.g. Lore) left few results, and `next/image`'s `sizes` hint (assuming a fixed ~180px card) then served a too-small optimized image the browser upscaled — visibly blurring the one oversized card; switched to `auto-fill`/`minmax(150px,200px)` so cards stay a fixed size regardless of result count. `WorldbuildingLoreReader` (the "blog"/article reading experience) and the Home Devlog feed were explicitly left untouched. `GalleryModal.tsx` and `WorldbuildingArtworkDetail.tsx` were deleted (fully superseded). Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same pre-existing `<img>`/hook-dependency warning pattern, no new categories), `npm run build` (all routes build), migration applied via `drizzle-kit migrate`. **In-browser QA (Preview deployments) found and fixed two real hero-sizing bugs the type/build checks couldn't catch:** (1) `width:auto` on the hero element let the browser's replaced-element sizing shrink the box itself for wide/cinematic images once `max-width` clamped it, silently defeating `object-fit` — changed to `width:100%`; (2) the hero height formula double-subtracted the scroll container's padding (a flex item's percentage height already resolves against the *content* box, which excludes padding) and separately fell 8px short of the visible viewport because `padding-bottom` isn't actually adjacent to the hero in a scrolling flex column (it only applies once, after all content) — corrected to `height:calc(100% + 8px)` (verified via `getBoundingClientRect` measurements in a live Preview deployment: top/bottom gaps now exactly 24px/24px, next media item's top edge measured below the visible viewport bottom pre-scroll). Re-verified after both fixes: single-image flush mode (`Character Spotlight`, all 4 edges pixel-equal to the scroll container), multi-image hero mode (`Mega Metropolis`), Worldbuilding Lore Reader unaffected (`Winter Pillars`), the `auto-fill` grid-sizing fix (Lore filter → one normal-sized, sharp card, not the previous oversized/blurred one), and Portfolio/Sketches/3D/Games lightboxes (including Games' Features list and Links, the highest-risk `richReading` path) — all confirmed on live Vercel Preview deployments, zero console errors. Map-pin click interaction itself could not be exercised via browser automation (the same environment limitation already documented under Task 4.6/4.9 for this exact map canvas), but the pin popup now renders through the identical, already-verified `MediaLightbox` single-image code path. **Faz 2 (post-close, same hotfix track):** **Complete.** New `displayTemplate` ('gallery'|'blog') column on `worldbuildingEntries` decouples the rendering shell from Entity Type — `WorldbuildingBrowser` now branches on `displayTemplate === "blog"` instead of the old `entityType === "lore"` check, so e.g. `entityType=location` + `displayTemplate=blog` is now representable (an entry can be classified as a Location and still publish through the article reader). New Worldbuilding Metadata admin page (`/admin/worldbuilding/metadata`) manages three groups — Entity Type, Category (legacy), Chips — as reusable `metadata_options` rows via a new `worldbuilding_metadata_options` junction table, mirroring the existing Task 2.9/2.10 Portfolio metadata pattern exactly but kept in fully separate modules (`lib/worldbuilding-metadata.ts`, `lib/actions/worldbuilding-metadata.ts`, `WorldbuildingMetadataForm`) so Portfolio's own metadata system was never touched. Entity Type is now fully dynamic/admin-managed: the public filter tabs and every category-badge label resolve through a live slug→name map built from active `wb_entity_type` options (`resolveEntityTypeLabel`'s new optional third parameter) instead of the old hardcoded `WORLDBUILDING_ENTITY_TYPES`/`ENTITY_TYPE_LABELS`; those constants remain only as the migration's seed values. `cat`/`entityType`/`chips` stay a dual-write mirror of the selected options' name/slug (same legacy-compatibility strategy Portfolio already established) so every existing public read path needed zero changes. A new idempotent `scripts/backfill-worldbuilding-metadata.ts` (`npm run db:backfill-worldbuilding-metadata`) links every existing entry's legacy `entityType`/`cat`/`chips` values to real managed options — run against both configured databases, created the 5 seeded Entity Type + 6 seeded Category options plus 16-17 chip options actually in use, and linked 20-28 junction rows across the existing entries with zero data loss (verified: an edit-page reopen after backfill shows every previously-set value pre-selected, not blank). `WorldbuildingLoreReader` was promoted to `components/shared/BlogReader.tsx` (same file, same behavior, new shared location) and the identical Gallery/Blog `displayTemplate` choice was added to Portfolio, Sketches, 3D, and Games (their own `displayTemplate` column each; each Browser component now branches to `BlogReader` when set to `"blog"`), completing the "same choice everywhere" request. **Also fixed the same trip:** `.wb-disc-grid` now caps at a real maximum of 7 columns at any viewport width (`minmax(max(150px, calc((100% - 24px)/7)), 1fr)`, replacing the earlier Faz 1 fixed 200px cap which still allowed 8 on very wide screens) — verified live via `getComputedStyle` on a 2048px-wide Preview deployment (exactly 7 equal ~278px tracks, 8th card wraps). Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same pre-existing warning pattern), `npm run build` (all routes including the new `/admin/worldbuilding/metadata` build), migration `drizzle/0025_harsh_captain_cross.sql` (additive: 5 `display_template` columns + check constraints, new junction table, 11 seed rows) applied to both configured databases (the same `.env` vs `.env.local` drift Task 4.5/4.7 already documented — worked around the same way). Public-site QA on a live Preview deployment confirmed: dynamic filter-tab labels render correctly (`All/Character/Location/Corporation/Technology/Lore` from live metadata, not the old hardcoded list), an unclassified entry's badge still correctly falls back to legacy `cat` text, the 7-column grid cap, and zero console errors. **Not exercised by automation:** the new admin Worldbuilding Metadata manager page and the Gallery/Blog selector UI in each admin form — authenticated Admin interaction is outside this session's permitted actions (same constraint noted in Task 4.7); the owner should manually verify adding/editing/deactivating a Chip/Category/Entity Type option, and picking "Blog" on a Portfolio/Sketches/3D/Games/Worldbuilding entry, against Production after deploy.
- **Site-wide thumbnail image quality fix (same hotfix track):** **Complete.** Root cause: every grid/card thumbnail site-wide (`WorldbuildingGrid`, `PortfolioBrowser`, `GamesBrowser`, `HomeWorldbuildingHighlights`, `ShowcaseCarousel`, `SelectedWorkCoverflow`, `HomeContactSocial`, `ArchiveList`) renders through `next/image`'s optimizer at Next's uncustomized default `quality: 75` (no component ever passed a `quality` prop), while `MediaLightbox`'s detail/lightbox view renders a raw, uncompressed `<img>` — this quality asymmetry, not a `sizes`/CSS mismatch (the Worldbuilding grid's own mismatch was already fixed and live), is what made every thumbnail look visibly softer than the same image's lightbox/reference version. `next.config.ts` now allows `images.qualities: [75, 90]` (additive — 75 stays the unaffected default for anything that doesn't ask for more) and `quality={90}` was added to all 8 thumbnail `next/image` calls; `MediaLightbox` was intentionally left untouched (already full quality, the correct reference behavior). Also corrected a minor, low-severity `sizes` underestimate on `HomeWorldbuildingHighlights`' first grid item (`62vw` → accurate `67vw` plus a `1020px` cap matching `.hwh-grid`'s actual 1512px-capped column fraction). Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same warning pattern), `npm run build`; live Preview deployment network trace confirmed `/_next/image` requests now carry `q=90`, and a zoomed side-by-side screenshot comparison of the exact two source images the owner flagged (a train scene and a metropolis scene, both used as real Worldbuilding entry cover images) showed a clear, visible sharpness improvement over the pre-fix `q=75` versions, with zero console errors.
- **Public Worldbuilding Discovery Rework (post-close):** **Complete.** Reworked `/worldbuilding`'s discovery area into one coherent browser with three top-level modes — ALL (default), ART, BLOG — sourced strictly from the persisted `displayTemplate` field (`gallery`→ART, `blog`→BLOG, ALL includes both), never `entityType`/`cat`. `WorldbuildingControls.tsx` was rewritten as the discovery shell: a compact `role="tablist"` mode row, a Steam-Market-inspired but visually restrained left filter rail (Search, Entity Type, Category, Chips — all sourced from real persisted data: `wb_entity_type`/`wb_category`/`wb_chip` managed options and the entry's own `cat`/`chips`) beside a dominant results column with a result count, and a "Clear Filters" action that resets only the secondary filters, never the mode. `WorldbuildingBrowser.tsx` composes mode + Entity Type + Category + Chips (AND across chips) + search (`fuzzyMatch` on title/excerpt, scoped to the active mode) predictably; routing is unchanged — a record's `displayTemplate` (not the discovery card style) still decides Gallery vs. Blog detail rendering, including for a Blog record found while in ALL mode. `WorldbuildingGrid.tsx` kept the existing media-first square-thumbnail card (`.wb-disc-grid`/`.wb-disc-card`) as the single shared geometry for all three modes — max columns lowered from 7 to 6 site-wide (`minmax(max(150px, (100%-20px)/6), 1fr)`), a 2-line title clamp added, and BLOG mode adds only a 3-line-clamped real `excerpt` under the title (no separate wide blog layout, no fabricated excerpts — entries with no excerpt simply omit the block); mode-aware empty-state copy (with a Clear Filters action when filters are active) replaces the old single message. Mobile/tablet (`≤900px`) collapses the rail behind a "Filters" toggle while the mode row stays permanently visible and reachable; `EmptyState` gained an optional `action` slot to host that Clear Filters button. No schema/migration, Admin, Map, or detail-viewer changes. Verified: `npx tsc --noEmit` (0 errors), `npm run lint` (0 errors, same pre-existing warning set, no new categories), `npm run build` (all routes), `npm run qa:responsive` (4/4 at 390×844/768×1024/1440×900/1920×1080, 0 overflow/console errors) all passed; live-browser verification via the Chrome extension confirmed mode filtering, category/chip/search composition, Clear Filters, and the empty state against real data (a local dev-database entry was temporarily flipped to `displayTemplate="blog"` to exercise BLOG/ART membership and the excerpt block, then reverted — confirmed via a direct DB re-read, zero net data change). `git diff --check` clean; only the discovery-area files above changed.
- Remaining non-blocking Home/site aesthetic enhancements: deferred to later polish / post-core implementation.
- Hero background rotation, advanced visual effects, and 3D/WebGL: not Sprint 3 blockers.
- The Admin Panel, CRUD systems, and public map foundation already exist and are being extended, not built from zero.
- No second Worldbuilding universe is planned scope.

## 9. Roadmap Update Protocol

At the end of every completed sprint: update the sprint status, record the next active sprint, add only important scope changes, move deferred features to the correct future sprint. Do not rewrite the entire roadmap. Do not duplicate information already documented elsewhere.

## 10. Scope Lock (Sprint 2.5 and Beyond)

- No new sprint will be added until Sprint 10 is complete, or the owner explicitly marks it skipped/closed (Sprint 10 is itself post-launch and optional — see section 3 — so this rule cannot silently block all future scope forever waiting on an optional sprint that may never start).
- No new major systems will be brought into the current development scope.
- Non-essential new ideas are parked as post-launch backlog, not adopted as current requirements.
- Public user accounts, social feeds, likes, comments, follow systems, marketplaces, and community systems remain out of scope.
- After Sprint 10, new scope is only considered based on a genuine, demonstrated usage need.

## 11. Pending Documentation Decisions

- **Content status model** — `01_INFORMATION_ARCHITECTURE.md` (section 14) and `02_CONTENT_ARCHITECTURE.md` (section 7) both describe a `Draft → Private/Review → Published → Featured → Archived` lifecycle with separate Editorial status, Visibility, and Featured-boolean concepts, but no current schema or admin system implements it yet. This needs an explicit owner decision (how the three concepts relate, and whether Featured stays boolean-per-item or becomes an ordered selection per Sprint 3's "at most 5–6 Featured Works") before any future content-model/status implementation work — not decided or scoped here.
